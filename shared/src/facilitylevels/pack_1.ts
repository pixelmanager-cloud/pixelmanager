// Facility level-narration pack 1. ONE author owns this file.
// Keys are facility keys; each array is indexed from LEVEL 2 (level 1 is the club's starting state), so
// entry [0] is what reaching level 2 looks like and entry [8] is level 10.
export const FAC_LEVEL_STORY_1: Record<string, string[]> = {};
