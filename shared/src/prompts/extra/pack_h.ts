// Authoring pack H. One author owns this file — nobody else writes to it, so packs never collide.
// Keys must match the bank they extend (see shared/src/prompts/*.ts for the key names and the house voice).
import type { Bank } from '../merge.js';
export const KIND_SETUP_H: Bank = {};
export const DEMAND_H: Bank = {};
export const FRAME_H: Bank = {};
export const SETTINGS_H: Bank = {};
export const CHILD_SETUP_H: Bank = {};
export const EVENT_PREFIX_H: Bank = {};
export const BIG_SETTINGS_H: string[] = [];
