// Authoring pack D. One author owns this file — nobody else writes to it, so packs never collide.
// Keys must match the bank they extend (see shared/src/prompts/*.ts for the key names and the house voice).
import type { Bank } from '../merge.js';
export const KIND_SETUP_D: Bank = {};
export const DEMAND_D: Bank = {};
export const FRAME_D: Bank = {};
export const SETTINGS_D: Bank = {};
export const CHILD_SETUP_D: Bank = {};
export const EVENT_PREFIX_D: Bank = {};
export const BIG_SETTINGS_D: string[] = [];
