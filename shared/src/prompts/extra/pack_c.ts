// Authoring pack C. One author owns this file — nobody else writes to it, so packs never collide.
// Keys must match the bank they extend (see shared/src/prompts/*.ts for the key names and the house voice).
import type { Bank } from '../merge.js';
export const KIND_SETUP_C: Bank = {};
export const DEMAND_C: Bank = {};
export const FRAME_C: Bank = {};
export const SETTINGS_C: Bank = {};
export const CHILD_SETUP_C: Bank = {};
export const EVENT_PREFIX_C: Bank = {};
export const BIG_SETTINGS_C: string[] = [];
