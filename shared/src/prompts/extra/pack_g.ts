// Authoring pack G. One author owns this file — nobody else writes to it, so packs never collide.
// Keys must match the bank they extend (see shared/src/prompts/*.ts for the key names and the house voice).
import type { Bank } from '../merge.js';
export const KIND_SETUP_G: Bank = {};
export const DEMAND_G: Bank = {};
export const FRAME_G: Bank = {};
export const SETTINGS_G: Bank = {};
export const CHILD_SETUP_G: Bank = {};
export const EVENT_PREFIX_G: Bank = {};
export const BIG_SETTINGS_G: string[] = [];
