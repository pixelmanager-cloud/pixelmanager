// Authoring pack J. One author owns this file — nobody else writes to it, so packs never collide.
// Keys must match the bank they extend (see shared/src/prompts/*.ts for the key names and the house voice).
import type { Bank } from '../merge.js';
export const KIND_SETUP_J: Bank = {};
export const DEMAND_J: Bank = {};
export const FRAME_J: Bank = {};
export const SETTINGS_J: Bank = {};
export const CHILD_SETUP_J: Bank = {};
export const EVENT_PREFIX_J: Bank = {};
export const BIG_SETTINGS_J: string[] = [];
