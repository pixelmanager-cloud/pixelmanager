// Authoring pack F. One author owns this file — nobody else writes to it, so packs never collide.
// Keys must match the bank they extend (see shared/src/prompts/*.ts for the key names and the house voice).
import type { Bank } from '../merge.js';
export const KIND_SETUP_F: Bank = {};
export const DEMAND_F: Bank = {};
export const FRAME_F: Bank = {};
export const SETTINGS_F: Bank = {};
export const CHILD_SETUP_F: Bank = {};
export const EVENT_PREFIX_F: Bank = {};
export const BIG_SETTINGS_F: string[] = [];
