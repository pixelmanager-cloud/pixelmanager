// Authoring pack B. One author owns this file — nobody else writes to it, so packs never collide.
// Keys must match the bank they extend (see shared/src/prompts/*.ts for the key names and the house voice).
import type { Bank } from '../merge.js';
export const KIND_SETUP_B: Bank = {};
export const DEMAND_B: Bank = {};
export const FRAME_B: Bank = {};
export const SETTINGS_B: Bank = {};
export const CHILD_SETUP_B: Bank = {};
export const EVENT_PREFIX_B: Bank = {};
export const BIG_SETTINGS_B: string[] = [];
