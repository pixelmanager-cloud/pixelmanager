// Authoring pack A. One author owns this file — nobody else writes to it, so packs never collide.
// Keys must match the bank they extend (see shared/src/prompts/*.ts for the key names and the house voice).
import type { Bank } from '../merge.js';
export const KIND_SETUP_A: Bank = {};
export const DEMAND_A: Bank = {};
export const FRAME_A: Bank = {};
export const SETTINGS_A: Bank = {};
export const CHILD_SETUP_A: Bank = {};
export const EVENT_PREFIX_A: Bank = {};
export const BIG_SETTINGS_A: string[] = [];
