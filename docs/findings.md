# Findings ledger

The audit factory's spine. Append-only: entries are closed, never deleted, so a later wave can be told what
is already known and does not spend its budget rediscovering it.

**Status:** `open` (confirmed, not yet fixed) · `fixed` (merged, with the probe that guards it) ·
`decision` (in `decisions-for-ck.md`, waiting on CK) · `refuted` (an agent claimed it, verification killed
it — kept so it is not re-raised).

**IDs** are `F-###`, assigned in order and never reused.

| id | status | cell | title | where |
|---|---|---|---|---|
| F-001 | fixed | dynasty/dead-wiring | The Family Record omitted every ancestor of the played line — `bloodline()` built from tokens, but `succeed()` reworks the played token in place | `client/src/api.ts` |
| F-002 | fixed | ui/accessibility | Reduce Motion deleted every toast in the game and the goal announcement, rather than calming them | `client/index.html` |
| F-003 | fixed | match/correctness | Conceding was celebrated exactly as hard as scoring — `mySide` was never consulted | `client/src/main.ts` |
| F-004 | fixed | ui/dead-wiring | Four `transition: width` rules had never once fired; the nodes are rebuilt by innerHTML each render | `client/src/main.ts` |
| F-005 | fixed | dynasty/visual | The heir card was hard-coded `tier-bronze`, the one tier that never turns the sheen layer on | `client/src/main.ts` |
| F-006 | fixed | ui/dead-wiring | `scorepulse` was declared twice and overridden by `scorepop` — unreachable | `client/index.html` |
| F-007 | fixed | ui/visual | Portraits hashed `name + band`, so a player changes identity when he crosses an age band. Fix written; blocked on the parallel 800-per-band pools landing | `client/src/portrait.ts` |
| F-008 | fixed | ui/visual | Portraits now shown in the squad stats table; lineup/market still pending (lineup, squad table, transfer market) — the Living Squad players are characters rendered as text rows | `client/src/main.ts` |
| F-009 | fixed | ui/accessibility | The game loads its two typefaces from Google Fonts; an offline Steam build falls back to Courier New (§78) | `client/index.html:9` |
| F-010 | closed (§82) | manager/correctness | Width, formation and duty choices barely register in outcomes — the ×12 breakaway is the engine's only real scoring channel (§68) | `shared/src/engine.ts` |
| F-011 | closed (§83) | ui/visual | Steam capsule art is AI-generated and publicly disclosed on the store page (§80) | `store/steam/` |
| F-012 | fixed | content/content | The existing 64 portraits contain 5 genuine near-duplicate pairs (colour distance < 12 on a face crop) | `client/public/portraits/` |
| F-013 | fixed | ui/dead-wiring | The IN-GAME "Reduce motion" switch still deleted every toast and the goal announcement — F-002 patched only the OS media query, not the `body.reduced-motion` twin | `client/index.html:283` |
| F-014 | fixed | content/dead-wiring | 92 authored commentary lines printed a literal `{opp}` — 23 of 24 draw sites never supplied it, and `fillCm` leaves unknown tokens intact | `client/src/main.ts:5317` |
| F-015 | closed (§84, answered: took (a), the final summer) | career/dead-wiring | The whole `Establishing` summer bank is unreachable — `rollFocus` only ever sees the chapter that just ENDED, and Establishing never ends before the career does. 8 authored options never reach a player | `shared/src/career.ts:1490` |
| F-016 | fixed | manager/dead-wiring | Every season-rollover feed line is stamped with the closing season, so none is ever rendered — promotion, relegation, the title, the near-miss | `client/src/main.ts:2658` |
| F-017 | fixed (F-030/F-031) | dynasty/correctness | Renown falls, against an in-game promise that it never can. The lineageRenown suspicion recorded here is **refuted**: the generation count does not move at the graduation drop. Two real mechanisms, both in F-030/F-031 | `client/src/api.ts:331` |
| F-018 | fixed | dynasty/dead-wiring | The played heir's genes are computed by `mintHeirs` and discarded, so the pre-selected son does not carry the family traits | `client/src/api.ts:704` |
| F-019 | fixed | economy/dead-wiring | `evaluateContractOffer` computes `moraleDelta` and no caller applies it — lowballing the star is free, and 'Generous' is identical to 'Meet it' | `shared/src/contracts.ts:106` |
| F-020 | fixed | audio/dead-wiring | `audio.unlock()` is bound to `pointerdown` only, so a keyboard or controller player gets no music and no chimes for the whole session | `client/src/main.ts:449` |
| F-021 | fixed | career/dead-wiring | Summer tiles label the authority meter "🧑‍🏫 Coach" on the three senior screens where the dashboard calls it "👔 Gaffer"; the legend then lists it twice | `client/src/main.ts:4083` |
| F-022 | closed (§85, answered: leave) | match/dead-wiring | `resolveShot`'s `clear` parameter is `false` at every call site, so the clear-chance bonus and its miss-logging branch are unreachable | `shared/src/engine.ts:881` |
| F-023 | fixed | save/dead-wiring | `IndexedDBBackend.open()` caches a rejected promise forever, so the retry its own comment describes cannot succeed | `client/src/save.ts:488` |
| F-024 | part fixed, rest §86 | content/dead-wiring | Three of five MILESTONE flourish banks are unreachable; the backroom-staff quip corpus and the ~305-line international call-up corpus have no production caller | `shared/src/tokens.ts:174` |
| F-025 | fixed | save/dead-wiring | `recoverOrphanedSaves` restores only the save index, so a recovered dynasty returns in the bottom division at manager season 1 | `client/src/main.ts:474` |
| F-026 | fixed | manager/dead-wiring | `PICKABLE_FORMATIONS` is a stale eight-formation list with no consumers; the editor offers all eleven from `FORMATION_SHAPES` | `shared/src/game.ts:80` |
| F-027 | fixed | manager/correctness | `maybeOfferArc` paces on `results.length` but the rollover never reset `arcLastMd`, so from season 2 the gate demanded matchday 21 of a possible 18 and no arc could be offered again | `client/src/main.ts:3601` |
| F-028 | fixed | manager/correctness | The prestige rank-up line is stamped with the CLOSING season, so the 423-line bank is still never displayed; `lastRankIdx` banks immediately so it cannot re-fire | `client/src/main.ts:2619` |
| F-029 | fixed | economy/correctness | The transfer market quotes a sale without `moraleEffects().sellMult` and credits a different number — wrong even at the default morale | `client/src/main.ts:1697` |
| F-030 | fixed | dynasty/correctness | `membersOf` scored only live tokens, and the played line is one token reused — so `rebornFields` zeroing `ach_*` took the retiring man's silverware out of the house at every succession | `client/src/api.ts:331` |
| F-031 | fixed | dynasty/correctness | `rebornFields` did not clear `career_honours_json`, so an heir was scored on his father's frozen record until he graduated — **the real mechanism behind F-017's graduation drop** | `shared/src/tokens.ts:577` |
| F-032 | fixed | dynasty/correctness | `membersOf` preferred the graduation-frozen peak over the live one, so a decade of development scored nothing and the `??` fallback was unreachable | `client/src/api.ts:344` |
| F-033 | fixed | dynasty/correctness | `succeed()` minted heirs from the father's BIRTH genes and omitted `ceilingLift`, leaving `legacyBoost().ceilingLift` with no production consumer — **a regression introduced by the F-013 family-resemblance fix** | `client/src/api.ts:748` |
| F-034 | closed (§88, answered: leave) | match/correctness | `beatsLastDefender` tests the NEAREST defender, not the deepest: 95.7% of the clear chances it grants have ~3.7 defenders still goal-side. Comments and UI copy corrected; the geometry change is a balance call | `shared/src/engine.ts:840` |
| F-035 | closed (§89, answered: taken) | match/correctness | The counter-attack window arms on any loose-ball pickup, including by the side that lost it — 32.9% of all armings involve no turnover at all | `shared/src/engine.ts:299` |
| F-036 | fixed | career/correctness | The career tutorial set its done-flag one turn before chapter 1 ended, so the focus / draft / coach hints written for the summer break could never render | `client/src/main.ts:3432` |
| F-037 | fixed | save/correctness | `rebuiltMgrState`'s tier restore reads the newest honour, but cup honours carry an empty tier and share the league honour's season — so an evicted save rebuilt in the bottom division | `client/src/main.ts:1443` |
| F-038 | fixed | ui/correctness | The Transfer Market wage-bill header billed the bloodline star and loan trialists, neither of whom is ever charged | `client/src/main.ts:1699` |
| F-039 | fixed | ui/correctness | The player card's Re-sign/Extend button ignored clicks landing on its sprite icon — the handler read the raw event target with no `closest()` | `client/src/main.ts:1045` |
| F-040 | fixed | content/correctness | `contract_renewed` passed the literal 'another spell' into an `{n}` duration slot, producing 'signs on for another another spell' across ~20 lines | `client/src/main.ts:1830` |
| F-041 | fixed | content/correctness | `personCtx` computed `seasonsAtClub` by subtracting MgrState.season from a profile-season stamp, so from generation 2 every squad player narrated as a newcomer | `client/src/main.ts:3552` |
| F-042 | fixed | economy/correctness | `facilityToDowngrade` picked the highest-LEVEL facility while its docstring promised the most expensive to run — the club shed the wrong department and recovered less | `shared/src/facilities.ts:255` |
| F-043 | fixed | manager/correctness | The four season-rollover climb events passed `person: undefined`, so 30 person-tiered promotion/relegation/title lines were unreachable — plus a fifth site, `scout_dispatched` | `client/src/main.ts:2735` |
| F-044 | fixed | economy/correctness | Facility cards priced every level at weight 1 while the club is billed with UPKEEP_WEIGHT — a L10 women's team advertised 567c and was billed 255c | `client/src/api.ts:1600` |
| F-045 | fixed | content/correctness | The Gaffer's Diary hard-coded a blank opponent, so its two highest-weighted storylines (13 lines) could never be selected | `client/src/main.ts:1288` |
| F-046 | fixed | save/correctness | `handoffKey` resolved the same key for the founder and the first heir, so deferring one offer suppressed the other | `client/src/main.ts:3947` |
| F-047 | fixed | ui/correctness | The Continental Cup toasted a hard-coded +1,800c while crediting at least 2,280c | `client/src/main.ts:2255` |
| F-048 | fixed | content/correctness | Three `near_miss` lines described play-offs and a runners-up finish in a pyramid that has neither, on a branch that can only be 3rd or 4th | `shared/src/manager/pack_1.ts` |
| F-049 | fixed | ui/correctness | `.tac-toggle` was emitted as a styling hook and defined nowhere, so both instruction checkboxes inherited a caption-above-a-select column layout | `client/index.html:1398` |
| F-050 | fixed | economy/correctness | The loyalty discount existed in three copies; the two that ran were inlined, while `staking.ts`'s version sat unimported naming the exact call sites ignoring it | `shared/src/contracts.ts:65` |
| F-051 | fixed | dynasty/ux-friction | Taking the reins while already managing silently wiped the live season, sponsor and cup run and swapped the bloodline star — no confirm, and the on-screen note pushed toward the button | `client/src/main.ts:4006` |
| F-052 | fixed | manager/ux-friction | The squad report's ✕ destroyed the only UI that can renew expiring contracts, with no confirm and no way back | `client/src/main.ts:2094` |
| F-053 | fixed | economy/ux-friction | Players force-sold at 60% to cover an unpayable wage bill were reported as contract expiries — "their deals ran out and weren't renewed" | `client/src/api.ts:1166` |
| F-054 | fixed | match/ux-friction | The pause menu's "▶ Resume" restored the prior state, so it did not resume a match paused with the spacebar | `client/src/main.ts:747` |
| F-055 | fixed | match/ux-friction | The full-time card auto-dismissed after 9s while instructing "▶ Tap to continue"; the match report exists nowhere else | `client/src/main.ts:5336` |
| F-056 | fixed | ui/ux-friction | Settings neither paused the match nor blocked the match shortcuts, and Pause → Settings actively un-paused | `client/src/main.ts:578` |
| F-057 | fixed | ui/ux-friction | The season prize money was the first of five toasts into a single 2.2s slot with no queue, and had no feed fallback | `client/src/main.ts:2617` |
| F-058 | fixed | save/ux-friction | `refreshHubPlayer`'s early return fired on any truthy starId, so the eviction-recovery block behind `!mgr.starId` could never run | `client/src/main.ts:1361` |
| F-059 | fixed | audio/ux-friction | The trophy fanfare sat on the MUSIC bus, so "Mute sound effects" left it playing and "Mute music" silenced it — both switches did the opposite of their label | `client/src/audio.ts:116` |
| F-060 | fixed | audio/ux-friction | Winning a division below the top tier fired the triumph sting and chime TWICE in one synchronous tick — both branches match, with no return between them | `client/src/main.ts:2795` |
| F-061 | fixed | audio/ux-friction | The career screen keyed its music on `momentKind === 'life'`, true for EVERY social turn, so the crisis pool played over routine turns — and over nothing else for the first 28 | `client/src/main.ts:4057` |
| F-062 | fixed | ui/accessibility | Settings and How To Play were the only overlays never converted to `dialogify`: no focus move, no Tab trap, the page behind them reachable | `client/src/main.ts:578` |
| F-063 | fixed | career/ux-friction | The financial-offer tile printed a negative branch for greed and form but not marketability, so the develop option's only cost was invisible | `client/src/main.ts:4134` |
| F-064 | fixed | ui/ux-friction | The hire-a-coach confirmation printed "He stays with the club for good." twice in the same dialog | `client/src/main.ts:2107` |
| F-065 | fixed | ui/ux-friction | Three persistent coin readouts printed raw integers beside copy that formats the same value with thousands separators | `client/src/main.ts:1314` |
| F-066 | fixed | career/ux-friction | The summer focus tile said choosing it "ends pre-season" with four-plus pre-season screens still to come | `client/src/main.ts:4184` |
| F-067 | fixed | dynasty/visual | The heir-choice cards had NO typography: all five `.cg-cname` and three `.cg-cdescr` rules are scoped to parents the heir card does not have | `client/index.html:1014` |
| F-068 | fixed | career/visual | `.cg-rival-news` is a div inside a `display:flex` row, so the news sentence became a chip on the header line | `client/index.html:685` |
| F-069 | fixed | match/ux-friction | The commentary feed hard-snapped to the bottom on every line, so the player could never read back — ~700 lines into a nine-line window | `client/src/main.ts:5634` |
| F-070 | fixed | manager/ux-friction | The shirt-sponsor deadline existed only in a code comment; a 450c deal vanished the moment matchday 1 was played, with nothing connecting the two | `client/src/main.ts:2206` |
| F-071 | fixed | dynasty/ux-friction | A cousin heir carries his own father's inheritance, not the one just chosen, and neither screen said so | `client/src/main.ts:3824` |
| F-072 | fixed | dynasty/ux-friction | The Family Record promised every pale forebear's line "can still be taken up" when only the current generation's brothers can | `client/src/main.ts:4017` |
| F-073 | fixed | career/ux-friction | `openCareer` discarded the `replayIssue` the API computes so the UI can explain a truncated career; the player saw a reset star and was told nothing | `client/src/main.ts:3489` |
| F-074 | closed (§90, answered: all three styled) | ui/visual | Four standalone containers render unstyled (`.scout-board`, `.sf-leaders`, `.li-tip`, `.ach-txt`); giving them a look is a design pass | `tools/playtest/css_hooks.ts` |
| F-075 | fixed | career/visual | The Player-tab position chip inherited pale text over the global `.role-*` backgrounds — GK 1.20:1, MF 1.33:1, FW 1.84:1, DF 2.29:1, all below the 3:1 non-text floor | `client/index.html:1077` |
| F-076 | fixed | career/visual | The rarity badge painted over the card name: 9 of 47 rare/epic names collided in the hand, 24 of 47 in the deck view | `client/index.html:1009` |
| F-077 | fixed | career/visual | The `.cg-graduation` staged arrival held back `#cg-reveal`, which three of its four screens do not have | `client/index.html:636` |
| F-078 | fixed | match/visual | The full-time card was `--good` green for every result — a 0-5 defeat read as a victory screen, against the sheet's own semantic-colour law | `client/index.html:1730` |
| F-079 | fixed | economy/visual | `#me-coins` had no CSS rule, so the hub coin balance rendered at the 22px body default beside readouts at 17px and 15px, on its own line | `client/index.html:475` |
| F-080 | fixed | save/correctness | `deleteSave` swallowed a rejecting durable delete, then swept localStorage, dropped the index row and toasted "Save deleted" — and `recoverOrphanedSaves` brought the save back at the next launch with its whole localStorage half destroyed | `client/src/main.ts:547` |
| F-081 | fixed | manager/correctness | The board graded the finished season against an expectation derived FROM that finish, not the one the player was shown all season | `client/src/main.ts:2624` |
| F-082 | fixed | manager/correctness | Every simmed fixture in a save was handed the save-constant league seed as its MatchEngine seed, so no season or matchday input reached it | `client/src/main.ts:2551` |
| F-083 | fixed | content/correctness | A one-boy youth intake — 19% of intakes — drew from a 140-line bank written entirely in the plural: "1 boys sign scholarship forms" | `client/src/main.ts:1784` |
| F-084 | fixed | ui/dead-wiring | The full-time card's "Gate receipts" line had no producer, so it could never render; gate money is already paid once a season | `client/src/main.ts:5241` |
| F-085 | fixed | save/dead-wiring | `SaveModel.retiredNumbers` was declared, defaulted and migrated with no reader or writer — retired shirt numbers lived only in localStorage | `client/src/save.ts:40` |
| F-086 | fixed | manager/correctness | On the retirement season the whole end-of-season report was filed under a season the player can never reach | `client/src/main.ts:2581` |
| F-087 | fixed | ui/ux-friction | The lineup screen labelled up to seventeen players "Bench:" when the engine can only ever bring on seven | `client/src/main.ts:5130` |
| F-088 | fixed | content/correctness | The gaffer's take threatened relegation in the bottom division, where the same screen says there is nowhere to go down to | `client/src/main.ts:2104` |
| F-089 | fixed | economy/ux-friction | Manager-arc choices moved up to 900 coins with no price on the button | `client/src/main.ts:3781` |
| F-090 | fixed | save/correctness | `newGame` and `continueSave` called `backend.save()` raw instead of `writeSlot()`, so a storage failure skipped the only code that sets saveHealth | `client/src/main.ts` |
| F-091 | fixed | save/ux-friction | The save list dated each save from a `lastPlayed` stamped only on open, never on play | `client/src/main.ts:514` |
| F-092 | fixed | manager/ux-friction | The Club Season screen quoted five prices with no coin balance anywhere — and the transfer market is an overlay, so both balances were on screen at once | `client/index.html:2006` |
| F-093 | fixed-for-real 2026-09-05 (landed on one screen only and stopped halfway on the other; see F-284/F-285) | ui/visual | Twelve measured visual defects: the morale ramp painted inside the fill, the minute stamp at 3.89:1 in a width-less column, four unequal "equal" columns, the transfer-market name crush, the goal announcement with no width cap, the Upgrade button at 3.33:1, locked achievements at 1.71:1 | `client/index.html` |
| F-094 | fixed | ui/visual | Media queries cannot see CSS `zoom`, so at uiScale 130% the main menu's compact block never fired and "Back" sat 20px past the fold (74px at 140%) | `client/index.html:229` |
| F-095 | fixed | manager/correctness | `withArcPrestige` rewrote the rank but not the next-rank fields, so the card read "→ the rank you already hold" | `client/src/main.ts:1284` |
| F-096 | closed (§91, answered: taken) | career/correctness | `social_storm` and `media`'s good branch have no youth-stage consequence — the fix is a balance change that turns `golden_replay` red | `shared/src/career.ts:794` |
| F-097 | fixed | dynasty/correctness | Taking a passed-over brother or cousin onto the line permanently subtracted his branch renown — measured 2802 → 2104 across two takes, on the panel that promises renown never falls | `client/src/api.ts:382` |
| F-098 | fixed | shared/determinism | The "no Date.now / no Math.random in shared/" rule was stated in four files and enforced by nothing; `shared_purity.ts` now enforces it by AST | `tools/playtest/shared_purity.ts` |
| F-099 | closed (§92, answered: Electron) | shared/determinism | 21 `Math.hypot` calls make the match engine implementation-dependent — a one-ULP difference changed 185 of 200 seeded matches. CK: going with Electron, which bundles one Chromium, so this cannot bite the Steam build | `shared/src/engine.ts` |
| F-100 | fixed (§89, CK) | match/correctness | The counter-attack window armed on any loose-ball pickup, including by the side that lost it — 1,062 armings/match → 621, 41% of every counter awarded with no turnover | `shared/src/engine.ts:291` |
| F-101 | fixed (§91, CK) | career/correctness | `social_storm` had no youth-stage consequence and `media`'s good outcome was worth nothing there; golden careers rebaselined (2 of 11 moved) | `shared/src/career.ts:794` |
| F-102 | fixed (§90, CK) | ui/visual | `.ach-txt` had no `flex: 1`/`min-width: 0` so long descriptions overflowed the plate; `.li-tip` rendered at the 22px body default | `client/index.html:1276` |
| F-103 | fixed (§84a, CK) | career/dead-wiring | The last chapter never got a summer — the finished-check sat above the boundary check — so all eight `Establishing` focus options were unreachable, including the only main summer focus that raises the sponsors meter | `shared/src/career.ts:1488` |
| F-104 | fixed (§86, CK) | content/dead-wiring | `first_start` wired at the Breakthrough boundary (4 lines; 2 deleted as unusable), `first_goal` kept permanently dead and now enforced by probe | `shared/src/tokens.ts:186` |
| F-105 | fixed (§86, CK) | content/dead-wiring | The 40 backroom-staff quips had no production caller — four staff were four fixed sentences for a whole dynasty | `client/src/main.ts:3351` |
| F-106 | fixed (§86, CK) | content/dead-wiring | `callUpBlurb`'s ~305-line corpus had no caller; one line now frozen into careerHonours and shown on the family tree | `shared/src/tokens.ts:575` |
| F-107 | fixed | ui/accessibility | The game had NO ARIA live region anywhere, so `#toast` — its only channel for errors, refusals and confirmations — was never announced | `client/index.html:2228` |
| F-108 | fixed | ui/accessibility | The main-menu save switcher rows were not keyboard-reachable; the only tab stop per row was the destructive Delete button, on a list labelled "Switch save" | `client/src/main.ts:543` |
| F-109 | fixed | career/accessibility | Every career render replaced `academy-body` wholesale, destroying keyboard focus on all 120 turns — restored, with a keyboard-only guard and no positional restore across a phase change | `client/src/main.ts:4453` |
| F-110 | fixed | match/accessibility | The full-time card had no focusable child, no Escape handler and unsafe centring, so it was mouse-only and clipped off-screen above 120% UI scale | `client/src/main.ts:5269` |
| F-111 | fixed | ui/performance | `appendLine` forced a synchronous layout per commentary line; `skipToEnd` drained ~464 of them in one burst | `client/src/main.ts:5911` |
| F-112 | fixed | content/correctness | `narrateRivalMoment` and `narrateCallupMoment` spliced the raw CARD TITLE into prose | `shared/src/narrate.ts` |
| F-113 | fixed | content/correctness | Seven authored manager-feed lines printed a literal `{fee}` / `{to}` at the player | `shared/src/managerNarrate.ts` |
| F-114 | fixed | content/correctness | "Someone in a {team} shirt" printed "a Ashcombe Town shirt" for 21% of the club pool, in the heaviest-used commentary bank | `shared/src/commentary/pack_1.ts:219` |
| F-115 | fixed-for-real 2026-09-05 (was recorded fixed on a half-wire; see F-280) | content/dead-wiring | `scout_found.young` and `.veteran` could never fire because `api.signMission`'s DTO dropped the player's age | `client/src/api.ts` |
| F-116 | fixed | economy/correctness | Gate receipts were paid for ten home matches in a nine-home season — `homeOf` rounded each of wins/draws/losses up independently; 71% of possible records overpaid | `shared/src/facilities.ts:395` |
| F-117 | fixed | save/correctness | `migrate()` guarded nine collections and skipped `club.players`, so a save that lost its squad throws in `fieldablePlayers` and will not load at all | `client/src/save.ts:128` |
| F-118 | fixed | career/performance | `pickArcStart` built the full 414-arc eligibility list before the cheap gate that discards it — on 82.5% of turns | `shared/src/storyarc.ts:109` |
| F-119 | fixed-for-real 2026-09-05 (the rule was present but outranked; see F-295) | economy/visual | The squad table's AGE column rendered near-black on the dark panel at ~1.1:1 — a `.age` rule stripped the background and never restored the colour | `client/index.html:1671` |
| F-120 | fixed | manager/accessibility | The Transfer Market was the last hand-rolled dialog: no inert, no focus move, no focus trap, so Tab walked out into the season screen behind it | `client/src/main.ts:1858` |
| F-121 | fixed | economy/accessibility | Every Upgrade, Scale back, Buy and Sell button announced as its verb alone — the subject lives in a sibling span with no `aria-labelledby` | `client/src/main.ts:3479` |
| F-122 | fixed | save/visual | A 17-character family name made the Continue button's text escape the button and vanish; and a corrupt save produced no visible feedback at all, its only message rendering below the fold | `client/index.html:250` |
| F-123 | fixed | ui/accessibility | All five Settings toggles were `role="switch"` divs with no accessible name — the knob is a `::after`, so they announced as "switch, on" and nothing else | `client/src/main.ts:697` |
| F-124 | fixed | ui/accessibility | Four higher-specificity `outline: none` rules cancelled the app-wide keyboard focus ring for every `<input>` in the game | `client/index.html:168` |
| F-125 | fixed | career/accessibility | `makeActivatable` ran on an unfiltered `[data-act]`, so ~20 view-only deck cards became fake buttons for an action `doCareerAct` rejects | `client/src/main.ts:4724` |
| F-126 | fixed | dynasty/correctness | `bloodline()` read an ancestor's ability off `card.peak ?? card.overall` — keys a LegacyCard has never carried — so every forebear on the family tree showed 0 | `client/src/api.ts:1433` |
| F-127 | fixed | content/correctness | Two `title` lines promoted the champions of the top flight to a division above it; a `friend_rivalry` prompt compared a man to himself | `shared/src/manager/pack_1.ts:1216` |
| F-128 | fixed (§93, CK) | economy/correctness | `played_loss` had no emitter and losing paid `played_draw`'s +2 — a regular at a club beaten every week for twelve years ended happier than the day he signed. Wired, and the delta re-tuned -1 → -3 | `shared/src/squad.ts:62` |
| F-129 | fixed | economy/dead-wiring | Four of staking.ts's five exports had no production caller, and `loyaltyProgress` could not return anything but 0 | `shared/src/staking.ts:23` |
| F-130 | fixed | economy/dead-wiring | `releaseClause` was priced on every render into a `sellValue` field that reached only an unreachable template branch | `shared/src/contracts.ts:57` |
| F-131 | fixed | save/correctness | `applyArcEffect` wrote the star's morale onto a per-call throwaway rebuilt by `tokenToPlayer`, so the change was discarded | `client/src/main.ts:3992` |
| F-132 | fixed | save/correctness | `inFlight` was a Set, so the first of two overlapping writes cleared the flag and a delete racing a write could leave the model on disk | `client/src/save.ts:581` |
| F-133 | fixed | audio/correctness | A volume or mute change during the 800ms crossfade was overwritten every frame and never re-applied — unmuting mid-fade appeared to do nothing | `client/src/audio.ts:179` |
| F-134 | fixed | manager/accessibility | The league table's promotion, relegation and champion zones existed only as CSS colour | `client/src/main.ts:1783` |
| F-135 | fixed | manager/accessibility | Both coin balances announced as a bare number — the label was a decorative SVG and a hover title; the training-focus select had no accessible name at all | `client/src/main.ts:2063` |
| F-136 | fixed | economy/accessibility | The star's morale was an 8px coloured dot with a title on a non-focusable span; the deal-length selector marked its choice with colour alone and threw focus away on every click | `client/src/main.ts:1217` |
| F-137 | fixed | manager/performance | `playerStats` accumulated one row per player per season for the life of the dynasty and nothing ever read a season older than the last | `client/src/save.ts:398` |
| F-138 | fixed | save/visual | The new-game club preview was a nowrap flex row in a 320px column; and `max-width: 96vw` resolves before CSS zoom, so at 110% the title panel was wider than the window below ~1010px | `client/index.html:257` |
| F-139 | fixed | career/accessibility | The six relationship meters exposed their value only as a CSS width and a `title` on a non-focusable div — now a real `progressbar` | `client/src/main.ts:3971` |
| F-140 | fixed | career/accessibility | The gaffer-temperament tiles never exposed which was selected, on a choice the code calls the one that gates which arcs fire | `client/src/main.ts:4567` |
| F-141 | fixed | match/visual | The squad-fitness label sat on the bar's own hue ramp at 1.66:1 worst case, with a text-shadow standing in for contrast | `client/index.html:1702` |
| F-142 | fixed | economy/accessibility | Squad-table sort headers were click-only — no tabindex, no role, no `aria-sort` — so a keyboard player could open any card but not sort | `client/src/main.ts:422` |
| F-143 | fixed | economy/accessibility | The player card's greed and fame meters carried their value only as an inline width on a 52px strip | `client/src/main.ts:1328` |
| F-144 | fixed | ui/accessibility | `makeActivatable` overwrote roles the markup had chosen — killing the temperament tiles' `role="radio"`/`aria-checked` and the sort headers' `aria-sort`, both shipped an hour earlier | `client/src/main.ts:5113` |
| F-145 | fixed | dynasty/correctness | The Trophy Room cabinet and the hub legacy line called `api.honours()` bare, and `honoursFor` defaults to the 30 most recent — the room the dynasty exists to fill truncates inside two generations | `client/src/main.ts:3780` |
| F-146 | fixed | manager/content | The pre-match presser hard-coded `competition: 'league'`, so a Continental Cup final and a World Finals semi got routine league-week prose | `client/src/main.ts:5454` |
| F-147 | fixed | audio/performance | A superseded crossfade never ran its cleanup, so a screen change during the 800ms fade left the outgoing track playing and stacked live audio elements in `tracked` | `client/src/audio.ts:188` |
| F-148 | fixed | manager/content | Four league-table prose banks and the Gaffer's Diary promised play-offs, knockout semi-finals and a penalty shoot-out in a pyramid whose entire promotion rule is top-two-automatic — 7 lines across 4 banks, plus a pool named `PLAYOFF_HUNT` that invited the next author to add more | `shared/src/gaffersDiary.ts:75` |
| F-149 | fixed | manager/content | A veteran renewal line read "He wanted two years. He got {n}." while {n} is a constant three seasons — the sentence treated a man getting MORE than he asked for as a climbdown | `shared/src/manager/pack_1.ts:830` |
| F-150 | fixed | match/content | Live match commentary keys player attributes by NAME, so ~78% of fixtures print at least one epithet earned by a different man | `client/src/main.ts:5868` |
| F-151 | fixed (§94 taken) | match/content | `pressConferenceLine`'s PRE_CUP pool — 57 authored lines — is unreachable: nothing in the game ever passes `competition: 'cup'` (→ decisions-for-ck §94) | `shared/src/press.ts:146` |
| F-152 | fixed | match/content | The PRE-match presser scores stakes from squad strength alone, so a Continental Cup tie is handed the 'routine fixture' value the post-match presser refuses to give it | `client/src/main.ts:5483` |
| F-153 | fixed | match/content | `goalLine`'s brace/hat-trick tally is keyed by name across both teams, so it announces braces and hat-tricks that did not happen | `client/src/main.ts:6303` |
| F-154 | fixed | match/content | The commentary corpus probe documents an orphan-key report it does not contain — the check cannot fail | `tools/playtest/commentary_corpus.ts:20` |
| F-155 | fixed | dynasty/content | The one-son succession copy is unreachable — `showHeirChoice` is only called when siblings exist, so `all.length === 1` can never be true | `client/src/main.ts:3423` |
| F-156 | fixed | dynasty/content | The Academy list bills every bought outsider as "first of the line" — `pedigreeText` is called without `isFounder`, the one argument that exists to stop this | `client/src/main.ts:3760` |
| F-157 | fixed | dynasty/content | Two of the three call-up banks (222 of ~305 lines) are unreachable — `careerHonours` hard-codes `scored: 0` and the cap count can never be 1 | `shared/src/tokens.ts:583, branching in shared/src/intl.ts:254-259` |
| F-158 | fixed | dynasty/content | `capLine` is never folded onto the legend card, so the international sentence vanishes at the succession and no forebear on the Family Record can ever show it | `client/src/api.ts:741` |
| F-159 | fixed | dynasty/content | Three `.star` manager banks (54 lines) have no reachable emit site — the bloodline player's arrival, sale and retirement never narrate as the bloodline player | `client/src/main.ts:2071, 2089 and 2128` |
| F-160 | fixed | dynasty/content | Passed-over brothers render as blank medallions on the Family Record, the exact outcome `branchCareer` was written to prevent | `client/src/main.ts:4483, 4523` |
| F-161 | fixed | career/determinism | Both cross-build replay gates contain an unreachable `st.phase === 'lifestyle'` branch, so no recorded career ever exercises `buyLifestyle` | `shared/qa_replay_contract.ts:53 and tools/playtest/golden_replay.ts:48` |
| F-162 | decision | career/determinism | `careerState`'s `clubLevel` argument has no producer anywhere, so `firstTeamReady`'s "a higher-level club is harder to break into" threshold is permanently 9 (→ decisions-for-ck §95) | `shared/src/tokens.ts:371` |
| F-163 | fixed (§96 taken) | career/determinism | The side-focus round is keyed on the chapter about to START while the main focus beside it is keyed on the chapter that just ENDED — confirmed, but the fix takes sponsor_meter from 57/150 to 98/150 against a <=90 bar (→ decisions-for-ck §96) — the exact bug F-103 fixed for the main pick | `shared/src/career.ts:1270` |
| F-164 | fixed | save/performance | The `missions` collection has no delete path anywhere in the tree, while every reader is scoped to the current season — so scouting rows accumulate in the save for the life of the dynasty, each carrying a full serialized Player | `client/src/save.ts:447` |
| F-165 | fixed | dynasty/accessibility | The Family Record SVG carried `role="img"` — ARIA Children Presentational — so every per-man `<title>` was deleted from the accessibility tree and a four-generation dynasty announced one sentence naming nobody | `client/src/main.ts:4544` |
| F-166 | fixed | dynasty/accessibility | The heir choice signalled the armed son by border colour alone: `role=button`, no pressed state, and a confirm button that never named him | `client/src/main.ts:4342` |
| F-167 | fixed | dynasty/accessibility | The succession chain replaced its panel three times without moving focus or announcing anything, destroying the focused tile each time | `client/src/main.ts:3393` |
| F-168 | fixed | match/determinism | The armed match plan was evaluated per animation FRAME in onFrame but per TICK in skipToEnd — setTactics re-derives press intensity, so a late evaluation desynchronises the rng stream: 53% different scorelines at 4x, 57% in a hidden tab | `client/src/main.ts:6114` |
| F-169 | fixed | match/determinism | simWorldCupTie and the World Finals shootout keyed their seed on `stage.length`, and 'qf'.length === 'sf'.length === 2 — the QF and SF of an edition printed the identical scoreline 52.6% of the time | `client/src/main.ts:2740` |
| F-170 | fixed | save/accessibility | The delete-forever confirmation was not a dialog and had no accessible name — a screen reader announced "Cancel, button" and never which bloodline was about to be destroyed | `client/src/main.ts:900` |
| F-171 | fixed | save/accessibility | Boot's post-recovery redraw lacked the New-Game guard its twin in quitToMenu has, so a first-launch player could be thrown out of the family-name field mid-type | `client/src/main.ts:513` |
| F-172 | fixed | save/accessibility | startNewGame's only failure message went to #login-error — not a live region, and measured below the fold — so a failed club creation was completely silent | `client/src/main.ts:985` |
| F-173 | fixed | save/accessibility | The family-name input had no label, and the sentence explaining what the name does was neither associated with it nor a live region | `client/index.html:2151` |
| F-174 | fixed | ui/determinism | A scouting trip was priced in matchdays but measured against a lifetime counter that only moves at the season rollover, so no trip could come home during the season it left in | `client/src/api.ts:314` |
| F-175 | fixed | ui/determinism | missions() filtered trips to the current profile.season, so a trip dropped off the Scouting screen at the exact rollover that revealed it — still live and signable, just invisible | `client/src/api.ts:1724` |
| F-176 | fixed | ui/determinism | The season screen's facility-gate arc re-offer was guarded by a flag its own synchronous call had already set, so the first screen of every session picked from a pool missing 30 arcs (36% divergence) | `client/src/main.ts:2292` |
| F-177 | fixed | ui/determinism | Settings → Credits and Settings → How to play restored this.running before opening their own overlay, so the match played on behind a full-screen dialog | `client/src/main.ts:803` |
| F-178 | fixed | dynasty/performance | The Family Record's draw-in stagger was linear in generation count with no clamp — the living star's medallion started at 4.42s by generation 15, and the ramp replayed on every Trophy Room visit | `client/index.html:1849` |
| F-179 | fixed | manager/content | narrateManager's line index had no per-event discriminator, so every facility upgrade, staff hire and scouting trip in a season printed a byte-identical sentence | `shared/src/managerNarrate.ts:141` |
| F-180 | fixed | career/content | `chapterRecap` picked its middle sentence from `RECAP_MIDDLE` with no chapter gate, and 8 of that bank's 16 lines name the club captain or the veteran mentor — so 48% of Grassroots/Academy recaps told an eleven-year-old that "Captain Oduya says the makings are there" | `shared/src/narrate.ts:4962` |
| F-181 | fixed | career/content | The three big-occasion beats (HUGE_TENSION, HUGE_AFTERMATH, BIG_BEAT) gated on raw stakes while their sibling `setting` gated on `bigOccasion`, so a high-stakes training session got cup-final prose — 85% of non-match stakes-2/3 beats | `shared/src/narrate.ts:3651` |
| F-182 | fixed | career/content | A lifestyle blurb addressed the player as "you" and then talked about him as "he" in one sentence — and `prompt_register`'s register check never saw it, because it loads seven prompt banks and none of career.ts's ~300 option strings | `shared/src/career.ts:396` |
| F-183 | open | career/content | The per-chapter `SETTINGS` banks are used for match AND non-match moments but are written as match settings ("At a stadium built in the eighties and not touched since"), so ~5% of training/social beats still open on a matchday scene. Found while narrowing the F-181 probe; it is a content project, not a gate | `shared/src/prompts/settings.ts` |
| F-184 | fixed | economy/content | The bloodline star's PersonCtx is blank on age, morale and seasons-at-club, so 53 authored transfer-bid lines can never fire | `client/src/main.ts:4234` |
| F-185 | fixed (§97 taken) | economy/content | The transfer market can never list a player over 32, so transfer_in.veteran's 18 lines about signing an old head are unreachable | `shared/src/transfermarket.ts:161 and shared/src/manager/pack_1.ts:512` |
| F-186 | fixed | economy/content | The Houses panel says renown makes "sponsorship and gate follow the family" — the multiplier touches neither, only the league finishing prize | `client/src/main.ts:4528` |
| F-187 | fixed | economy/content | 60 authored feed lines print coin amounts unformatted — fillMgr stringifies {fee} raw, so the season log reads "It costs 14000c" | `shared/src/managerNarrate.ts:134-137` |
| F-188 | fixed | economy/content | The Club Shop and Women's Team cards quote a fixed mid-table income, so a lower-division club is promised money it will not be paid | `shared/src/facilities.ts:319 and :321` |
| F-189 | fixed | ui/content | The Transfer Market's squad counter and both squad-size gates read the MERGED club, which is one bigger than the squad the engine bounds | `client/src/main.ts:2069` |
| F-190 | fixed | ui/content | The two capstone prestige achievements fire one rank below the rank they are named after, and reaching the actual top rank unlocks nothing | `shared/src/achievements.ts:69` |
| F-191 | fixed | ui/content | The KIT tab tells the player his nickname is "what the crowd and the commentary call him"; nothing anywhere reads it | `client/src/main.ts:5209` |
| F-192 | fixed | ui/content | The backroom-staff card announces a signing that did not happen — 'signing' is the club screen's default quip moment | `client/src/main.ts:3688` |
| F-193 | fixed | ui/content | How to play quotes the pre-weighting upkeep totals — 1,344c and 6,804c against an actual 1,198c and 6,067c | `client/src/main.ts:2300` |
| F-194 | fixed | manager/determinism | The World Finals edition is derived from the per-generation season counter, so every generation replays Edition 1 (and 2) with a byte-identical 16-nation field, draw and bracket | `client/src/main.ts:2716-2717` |
| F-195 | decision (→ §98) | manager/determinism | The transfer market is not generation-scoped, so an heir shops from the identical twelve players his father did — while the `houseListings` call on the line above is generation-scoped | `client/src/main.ts:2066` |
| F-196 | fixed | manager/determinism | Continental and World-Finals opponents pass the season/edition only in `generateClub`'s inert `id` argument, so the same eleven players and the same tactics turn out for every opponent, every season, forever | `client/src/main.ts:2659-2662 and 2787-2790` |
| F-197 | fixed | manager/determinism | `rebuiltMgrState` restores the manager's season from `profile.season`, a lifetime counter, while every consumer of `MgrState.season` treats it as the per-generation season | `client/src/main.ts:1781` |
| F-198 | decision (→ §99) | economy/performance | The squad table's star-row highlight is `filter: brightness(1.02)` — a 2–4/255 lift nobody can see, so `tr.nft-row` has no row-level treatment at all | `client/index.html:531` |
| F-199 | fixed | ui/correctness | An agent left a self-referential symlink `shared/shared -> shared` in the repo; git does not track it and it ships nothing, but `field_wiring`'s directory walk followed it until the OS threw ELOOP, failing the playtest leg | `tools/playtest/ship_hygiene.ts` |
| F-200 | fixed | career/content | `{RIVAL}` is filled with a personal surname, but 11 story-arc beats wrote it as a club — "the bitter rivals whose fans have jeered him", "{RIVAL} trudge off humiliated", "the last {RIVAL} defender". In one career the same name did both | `shared/src/storyarcs/signature.ts:71` |
| F-201 | fixed | match/dead-wiring | `runMatch` has no production caller — the client drives MatchEngine itself — and `types.ts` pointed readers at it as the writer of `homeBoost`, which is actually set by the team talk | `shared/src/types.ts:106` |
| F-202 | fixed | audio/performance | `sting()`'s `sfxMuted ? 0 : sfxVolume` had an unreachable true branch: the method already returns early when muted, so it read as a second mute gate that was never one | `client/src/audio.ts:131` |
| F-203 | fixed | ui/correctness | The client formats coins with a bare `toLocaleString()` (main.ts:311 and elsewhere), which is locale-dependent — a de-DE player sees "14.000c" in a toast beside "14,000c" in the feed, now that shared/ groups deterministically. Found while correcting F-204's comment | `client/src/main.ts:311` |
| F-204 | fixed | career/content | `ageFraming` never consulted the `age` it was passed — the chapter branch returns first — while 65 chapter frames open by declaring a specific age. Chapter bands are 2-3 years wide, so 68 of 110 age-stating prompts named an age contradicting the HUD header on the same screen | `shared/src/narrate.ts:3065` |
| F-205 | fixed | match/dead-wiring | simOneFixture applies the facility edges but not the three backroom-staff edges, so hired coaches do nothing in any simmed league fixture | `client/src/main.ts:3081-3090` |
| F-206 | fixed | match/dead-wiring | beatsLastDefender's comment announces a CHANCE_RANGE proximity gate that does not exist anywhere in the tree | `shared/src/engine.ts:877-893` |
| F-207 | fixed | match/performance | cpickNR token-fills every authored line in the bank on every draw and keeps one — 117,087 substitutions per match to render ~340 lines | `client/src/main.ts:6496` |
| F-208 | fixed | save/determinism | The localStorage-eviction recovery block in refreshHubPlayer is still unreachable — rebuiltMgrState() assigns starId before the block tests for its absence | `client/src/main.ts:1712` |
| F-209 | fixed | save/determinism | The transfer market's already-signed ledger is localStorage-only and generation-keyed, so the same listing can be bought twice and sit in the squad twice | `client/src/main.ts:2090 and :2145, against client/src/api.ts:577-580` |
| F-210 | fixed | economy/determinism | The Local Tryouts pool is seeded on the constant string 'local', so every save in the game sees the identical walk-ups | `client/src/api.ts:1665` |
| F-211 | fixed | economy/determinism | The two wage-bill forecasts disagree with each other and with the rollover about who is actually on the payroll | `client/src/main.ts:2448 and client/src/main.ts:2120` |
| F-212 | fixed | economy/determinism | The Commercial Dept card prints an income FLOOR the income routinely falls below — the last card in the file that copies a formula instead of calling it | `shared/src/facilities.ts:317` |
| F-213 | fixed | economy/determinism | The Commercial Dept blurb promises income "for every trophy in your cabinet"; only league titles are counted | `client/src/api.ts:991` |
| F-214 | fixed | dynasty/determinism | Every dynasty in the game names its heirs Leo → Milo → Enzo → Ravi → Jude → Yuki, in that order, in every save — rebornFields seeds the played heir's name off the token id, which is always `nft:1` | `shared/src/tokens.ts:640` |
| F-215 | fixed | dynasty/determinism | Seven of the twelve rival houses field a son with the identical first name in every generation, and four of them share a personality — the per-house discriminators in the seeds are `h.base` (2 distinct values) and `h.name.length` (5) | `shared/src/houses.ts:90` |
| F-216 | fixed | dynasty/determinism | The bought academy prospect has byte-identical genes and the same role in every save — the comment claiming the save id is mixed in sits between the two lines that don't mix it | `client/src/api.ts:290` |
| F-217 | fixed | dynasty/determinism | The family trait is re-rolled at every succession — `familyTrait` is keyed on the retiring star's per-generation career seed, not on anything that identifies the bloodline | `shared/src/bloodline.ts:77` |
| F-218 | fixed | content/ux-friction | The Life tab prints an endorsement payout in coins that nothing ever credits — up to ~17,900c, more than a whole career earns | `client/src/main.ts:4250` |
| F-219 | fixed (§100 taken) | content/ux-friction | The 🎲 TEMPTATION badge summons the player to a panel that poses a dilemma with no way to answer it | `client/src/main.ts:5106` |
| F-220 | fixed | content/ux-friction | The lifestyle shop hides the greed a purchase adds, while the offer tile eight lines above it discloses exactly that | `client/src/main.ts:5081` |
| F-221 | fixed (§101 taken) | audio/determinism | The match music pool cannot deliver the "no repeat within a fixture" it was bought for — play() picks ONE url and sets loop = true, so the pool only rotates between matches | `client/src/audio.ts:23` |
| F-222 | fixed | audio/determinism | The crossfade is driven by requestAnimationFrame, so hiding the window mid-fade leaves two soundtracks playing on top of each other — the one thing its own comment says can never happen | `client/src/audio.ts:218 and :221, against the promise at client/src/audio.ts:179-180` |
| F-223 | fixed | audio/content | The match-music comment claims the three-track pool gives "effectively no repeat within a fixture"; the pool is 355.7s not 494s, and play() loops ONE picked track for the whole match | `client/src/audio.ts:23` |
| F-224 | fixed | audio/content | Two comments assert the music credit is a licensing obligation and quote CREDITS.md as saying it "MUST appear" — CREDITS.md says the opposite, and the licence file records the correction and warns against restating it | `client/src/main.ts:794-796 and client/src/main.ts:2296-2297` |
| F-225 | fixed | audio/content | The audio drop-folder README ships inside the game bundle, with internal TODOs and "(title not recorded)" placeholders — and ship_hygiene's stray-file check cannot catch it | `client/public/audio/README.md:1` |
| F-226 | decision (→ §102) | audio/content | The whole manager layer — hub, lineup, club and season screens — is a one-track pool, so hub-1 is the loop a dynasty is played to for its entire length | `client/src/audio.ts:18` |
| F-227 | fixed | save/content | rebornFields never clears kit_json, so every heir inherits his father's squad number and nickname — against "his for life" and "no future player wears these" | `shared/src/tokens.ts:633` |
| F-228 | fixed | save/content | The storage-failure banner wraps a whole prose sentence in a parenthetical meant for a raw exception string | `client/src/main.ts:6880, against client/src/save.ts:631-633` |
| F-229 | fixed | content/determinism | Manager story arcs carry no generation in their seed, so an heir replays his father's exact arc sequence | `client/src/main.ts:4398` |
| F-230 | fixed | content/determinism | (closed by the genSeed sweep, F-238) The Continental Cup is seeded on the per-generation season, so every heir draws his father's opponents and reads his father's tie blurbs | `shared/src/intl.ts:60` |
| F-231 | fixed | content/determinism | The three continental rounds are drawn independently, so a quarter-final you knocked out comes back in the final | `shared/src/intl.ts:59` |
| F-232 | fixed | content/determinism | The pre-match presser's salt omits the season, so the manager says the same 18 sentences every season for the whole dynasty | `client/src/main.ts:5725` |
| F-233 | fixed | manager/visual | The season screen's two-button action row sizes only one button — `.sf-tm #sf-transfers` is keyed to an id, so "Team Sheet" renders 64% larger than "Transfer Market" beside it | `client/index.html:850` |
| F-234 | fixed | manager/visual | The World Finals panel's own buttons have no rule, so "Follow the tournament" renders at 18px beside the Continental Cup's identical 11px action one panel below | `client/index.html:962` |
| F-235 | decision (→ §103) | manager/visual | The training-focus dropdown inherits the global 21px `select` size inside a 14px row — the only select in the game never given a context size | `client/index.html:925` |
| F-236 | fixed | manager/visual | The shirt-sponsor cards inherit `text-transform: uppercase` from the global button rule, so their prose renders "+450C NOW, GUARANTEED" | `client/index.html:939` |
| F-237 | fixed | manager/visual | `#trophies .tm-crest` can never match — `.tm-crest` is emitted only inside the transfer-market overlay, which is appended to document.body | `client/index.html:1338` |
| F-238 | fixed | manager/determinism | THE CLASS, not the instances: 13 further sites mixed the save-constant `leagueSeed()` with `m.season`, which resets to 1 at every succession — the league table, the star bid, the board mood, the Continental Cup and its shoot-out. Five of these had been reported one at a time across three waves; a `genSeed()` helper and a probe now hold the rule | `client/src/main.ts:1932` |
| F-239 | fixed | manager/determinism | The POST-match presser was still on `leagueSeed()` after the pre-match one was fixed, so every heir gave his father's post-match answers word for word. Found by widening `generation_scoped_seeds` to a two-line window in BOTH directions | `client/src/main.ts:6394` |
| F-240 | fixed | audio/accessibility | The topbar mute button never exposes its own state — it announces "Toggle music, button" whether music is on or off | `client/index.html:2161` |
| F-241 | fixed | audio/accessibility | The SFX volume slider's "preview once on release" chime fires on every keyboard step, so arrowing the slider machine-guns overlapping chimes | `client/src/main.ts:836` |
| F-242 | fixed | audio/visual | The Music and Sound-effects sliders wear the text-field chrome: `.set-row input[type=range] { height: 8px }` can never render, and hover/focus paint an input border on them | `client/index.html:353` |
| F-243 | fixed | audio/visual | openSettings' docstring says SFX volume has not shipped yet, four lines above the code that renders it | `client/src/main.ts:770-771` |
| F-244 | fixed | content/accessibility | The moment's PRIMARY demand tag is distinguished from the secondary ones by hue alone, and the on-screen hint tells the player to read the colour | `client/src/main.ts:5073` |
| F-245 | fixed | content/accessibility | Draft cards announce "Play <card>" — cardHtml's accessible name hard-codes the play verb for every act except 'view' | `client/src/main.ts:5293-5295, reached from :5116` |
| F-246 | fixed | content/accessibility | The commentary 'lull' lines render at 3.93:1 on the ticker — the only one of the ticker's 23 colours below the 4.5:1 floor | `client/index.html:1741, emitted at client/src/main.ts:6783` |
| F-247 | decision (→ §104) | content/accessibility | After a career action the outcome narration is inserted above the focus target, so a screen-reader player is never told how the moment went | `client/src/main.ts:5055 and :5245, against :5494-5518` |
| F-248 | fixed | content/performance | mergeBanks rebuilds its dedupe set and reallocates the whole merged array once per (pack × key); mergeList, seven lines below, builds one set and pushes | `shared/src/prompts/merge.ts:17 and :24, against shared/src/prompts/merge.ts:31` |
| F-249 | fixed | content/visual | The gaffer-temperament tiles have no typography — every `.cg-cname`/`.cg-cdescr` rule is scoped to a parent `.cg-temper` does not have, the exact trap the sheet documents three lines above | `client/src/main.ts:4881` |
| F-250 | fixed | content/visual | `.cg-effs` inside a `.cg-coach` tile has no rule, so the retirement screen's effect line renders at the 22px body default — larger than the tile's own 14px name | `client/src/main.ts:3580-3581` |
| F-251 | fixed | content/visual | `.cm-lull` is the only commentary tier below the readable-text contrast floor — 3.93:1 where the other twenty-two sit between 6.05 and 13.87 | `client/index.html:1741` |
| F-252 | fixed | content/visual | `.cg-arc-story` can never win — `.cg-scenario .cg-story` outranks it on every element that carries it, so the rule is inert | `client/index.html:760` |
| F-253 | fixed | content/correctness | The Gaffer's Diary keeps announcing the rival breakthrough for the rest of the season — `rivalFirstWin` reads the last meeting with the rival, not the most recent match | `shared/src/gaffersDiary.ts:266-277` |
| F-254 | fixed | content/correctness | `narrateOffer`'s develop branch cannot fire — every offer in the game has `earn > 0`, so OFFER_DEV's 9 lines are unreachable and turning the money down narrates from the generic bank | `shared/src/narrate.ts:4840-4846` |
| F-255 | fixed | content/correctness | Life-event, rivalry and call-up beats narrate the BAD outcome while the mechanics pay the GOOD one — the narrator's threshold is 0.62, the consequence's is 0.55 | `shared/src/narrate.ts:4249` |
| F-256 | fixed | ui/accessibility | The Manager Legacy card can only be opened with a mouse — #me-prestige is a bare <span> with an onclick and no tab stop | `client/src/main.ts:1613` |
| F-257 | fixed | ui/accessibility | The KIT tab's two <label> elements are associated with nothing, so #kit-number has no accessible name at all | `client/src/main.ts:5396 and client/src/main.ts:5397` |
| F-258 | fixed | ui/accessibility | The lineup screen's two toggle widgets carry their on/off state in CSS colour only, and the match-plan tick announces as checked in BOTH states | `client/src/main.ts:5841` |
| F-259 | fixed | ui/accessibility | The "THE GAME IS NOT BEING SAVED" banner is injected into the DOM with no live region, so it is never announced | `client/src/main.ts:6972-6976` |
| F-260 | fixed | dynasty/correctness | membersOf grants the PLAYED line a fabricated `branchCareer`, so a newborn heir scores a full professional career the day he is born and the living star's real record is permanently discarded when it is smaller | `client/src/api.ts:415` |
| F-261 | fixed | dynasty/correctness | Every generation's legend card is banked with the DYNASTY-LIFETIME continental + World Finals cup count, so cups compound across generations | `client/src/main.ts:3628` |
| F-262 | fixed | dynasty/correctness | Taking a brother or cousin onto the line never demotes the pre-selected trunk heir, so two men on one rank are drawn as the played line and the Family Record centres the wrong one | `client/src/api.ts:1418` |
| F-263 | fixed | dynasty/correctness | The Trophy Room splits one family into two bloodlines after a branch switch and restarts its generation numbering at 1 | `client/src/main.ts:4046` |
| F-264 | fixed | save/correctness | migrate()'s team-sheet fallback rebuilds from the UN-repaired club, so a save with both a damaged squad and a lost sheet throws and can never load | `client/src/save.ts:151` |
| F-265 | fixed | ui/visual | §99 TAKEN: the star's squad-table row now carries a translucent overlay that composites over `statColor`'s inline background — measured ΔE00 1.95-7.41 against an ordinary row, above the 1.62 zebra-stripe floor, with adjacent ramp separation preserved at 90-101% | `client/index.html` |
| F-266 | fixed | manager/visual | §103 TAKEN: `.sf-focus select` sized to its row | `client/index.html` |
| F-267 | fixed | content/accessibility | §104 TAKEN: `.cg-narrate` is a `role="status"` region, so the outcome of a career turn is announced instead of the card focus lands on | `client/src/main.ts` |
| F-268 | fixed | manager/determinism | `generation_scoped_seeds` was GREEN over three real defects — a neighbouring `gen` argument, and then a COMMENT quoting `genSeed`, vouched for code that had none. Now anchors only on code, strips comments, and counts the window in CODE lines. Mutation sweep: 16 of 16 sites caught, was 13 | `tools/playtest/generation_scoped_seeds.ts` |
| F-269 | fixed | manager/content | A PRE_CONTINENTAL line taught "two legs changes everything" — the Continental Cup has no two-legged tie, CONT_ROUNDS is QF/SF/Final and each is settled in one game. Wrong before this change, exposed by making the bank beside it readable | `shared/src/extra/press_pack_1.ts:400` |
| F-270 | fixed | manager/correctness | The generation-scoped-seed probe is structurally blind to simRemainingFixtures, whose match seed mixes leagueSeed() with m.season and no generation | `client/src/main.ts:3230` |
| F-271 | fixed | manager/correctness | A simmed Continental Cup FINAL is scored as an away tie, on a tie the same function's comment and contOpponent both declare neutral | `client/src/main.ts:2813-2815` |
| F-272 | fixed | manager/correctness | The retirement send-off credits the retiring man with the whole DYNASTY's league titles and cups, while the legend card thirty lines later subtracts what his fathers already banked | `client/src/main.ts:3574` |
| F-273 | fixed | manager/correctness | Taking the reins with another line runs a partial copy of the succession reset, so the new star replays one World Finals edition and then has the next one suppressed entirely | `client/src/main.ts:5052-5058` |
| F-274 | fixed | manager/correctness | retireStar counts one season too many, because F-086's fix advances the counter immediately before it is read | `client/src/main.ts:3569, reached from client/src/main.ts:3464` |
| F-275 | fixed | economy/correctness | The Transfer Market's SELL column lists loan/trial players with a live "Sell · +Nc" button that api.sellPlayer always refuses | `client/src/main.ts:2179` |
| F-276 | fixed | economy/correctness | Gate receipts still pay for TEN home matches in a nine-home season on an unbeaten record — F-116's fix does not fully hold | `shared/src/facilities.ts:426-428` |
| F-277 | fixed | economy/correctness | The facility Upgrade button prints its price unformatted — "Upgrade · 💰 14000 ▶" beside its own aria-label and confirm dialog reading "14,000c" | `client/src/main.ts:3921` |
| F-278 | fixed | ui/correctness | The Transfer Market's SELL column lists loan trialists with a price, and selling one is refused by the facade | `client/src/main.ts:2179` |
| F-279 | fixed | ui/correctness | Signing a trialist with a full squad is reported to the player as "You've hit your loanee limit" — contradicting the 0/3 counter on the same screen | `client/src/main.ts:5796` |
| F-280 | fixed | content/dead-wiring | F-115 does not hold: api.signMission's DTO still drops the scouted player's age, so scout_found.young and scout_found.veteran remain unreachable | `client/src/api.ts:1963` |
| F-281 | decision (→ §105) | ui/ux-friction | The Trophy Room's "Retire #N" button permanently hangs a shirt up on one unconfirmed click, and the copy that says it is forever only appears afterwards | `client/src/main.ts:4182` |
| F-282 | fixed | ui/ux-friction | Contract talks offer "Lowball — he may walk" and report that he "walks away from the table", but nothing walks: the panel stays fully live and the only real cost, −6 morale, is never shown | `client/src/main.ts:1576` |
| F-283 | fixed | ui/ux-friction | The facility Upgrade button prints its price ungrouped while its own disabled twin, its aria-label and the confirm it opens all group the same number | `client/src/main.ts:3921` |
| F-284 | fixed | ui/visual | The Scouting screen's primary action is still white-on---accent at 3.33:1 — the exact ratio the sheet's own comment 45 lines earlier calls "under the floor", fixed on the Club screen only | `client/index.html:1587` |
| F-285 | fixed | ui/visual | F-093's locked-achievement fix moved 1.71:1 to ~3.0:1 and stopped there — still under the 4.5:1 floor, on all 20 locked rows | `client/index.html:1373` |
| F-286 | fixed | ui/visual | The trophy cabinet's reflection has never existed — the `::after` that is supposed to draw it declares only `content: ''` and `display: block`, and paints a 0px transparent box | `client/index.html:1357` |
| F-287 | fixed | ui/visual | An unaffordable contract offer stacks two opacity rules to 0.27, so "🔒 need 433c more" — the only place the shortfall is stated — renders at 1.85:1 | `client/index.html:915` |
| F-288 | fixed | dynasty/content | `heirAsPlayer` — the function written to make a passed-over brother "a full player, not a summary row" — has no production caller, and the shipped brother is exactly the summary row its comment says he is not | `shared/src/bloodline.ts:164-183` |
| F-289 | fixed | dynasty/content | `renownIncomeMult`'s docstring still claims renown pays "in sponsorship and gate" — the exact false mechanic F-186 was raised to delete, surviving in the function's own doc where the guard probe does not look | `shared/src/renown.ts:190` |
| F-290 | fixed | career/content | The 11 `kid`-tier season-event rewrites can never render — `flavorTier` is read after `turn++`, so it never sees Grassroots (also reported from the career/dead-wiring lens — one fix covers both) | `shared/src/career.ts:1730` |
| F-291 | fixed | career/content | The final chapter never gets a recap, so RECAP_OPENERS.Establishing's 13 authored lines are unreachable (also reported from the career/dead-wiring lens — one fix covers both) | `shared/src/narrate.ts:4969` |
| F-292 | fixed | career/content | Two career.ts comments state that zero arcs use `requires`, beside a live filter gating 116 authored choices (also reported from the career/dead-wiring lens — one fix covers both) | `shared/src/career.ts:1161 and shared/src/career.ts:1252-1253` |
| F-293 | fixed | career/content | The off-pitch fame gate's comment cites turn 90 and turn 150 against code that ramps 54→90 in a 120-turn career | `shared/src/offpitch.ts:90-93` |
| F-294 | fixed | manager/visual | The generic zebra-stripe rule overrides `.role-GK/DF/MF/FW` on the squad table's position chip, so every other row's GK/DF/MF/FW letters are near-black on near-black (1.13:1) | `client/index.html:188` |
| F-295 | fixed | manager/visual | F-119's fix never applied: `td.stat.age`'s colour is outranked by `table.squad td.stat`, so the AGE column is still near-black at 1.05:1 | `client/index.html:1801, against client/index.html:1679` |
