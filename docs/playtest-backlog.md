# Playtest backlog — continuous testing findings

The living issue list produced by the playtest harness. Newest findings on top. Status: `[ ]` open · `[~]` in progress · `[x]` fixed.

## The harness (how testing runs)

Three complementary layers, because different problems need different tools:

1. **Headless balance/content probes** (`tools/playtest/*.ts`, `npm run playtest`) — simulate hundreds–thousands
   of careers/seasons through the deterministic engine, no browser. Catch *balance/content* issues at scale
   (is skilled play rewarded? is the rival beatable? can you climb the pyramid? are titles winnable?). Fast,
   parallel-safe, run anywhere (incl. Vultr overnight). This is the 24/7 workhorse.
   - `analyze_player_career.ts` — skilled-vs-random policy across many careers.
   - `analyze_manager_career.ts` — dynasty climb up the 10-tier pyramid.
2. **Browser UX agent** (`.claude/agents/ux-playtester.md`, Opus) — actually plays the game in-browser and
   critiques *interface/UX/content-feel* from a fresh-player POV (the layer that found the core card bugs).
   Best run as periodic passes while a session is live (it drives the one Browser pane, so passes serialise).
3. **This backlog** — findings from both layers, deduped + triaged, that we then fix.

**Constraints (honest):** the headless probes are deterministic, so continuous value comes from *sweeping
new seeds/configs* each pass, not re-running identical checks. Browser agents share one pane → they run one
at a time, not truly parallel. True survive-the-Mac-sleeping 24/7 = run the headless layer on a Vultr VPS
(the browser layer needs the desktop app). See [[feedback-overnight-vultr-agents]].

---

## Open findings

_None from the harness right now — the two launch-pass balance flags are fixed. New findings land here from the rotating cron passes (browser UX agent + probes)._

<!-- Browser UX pass 2026-08-29 (player late-career → handoff → retirement → heir/succession RE-PASS) — reached the END of the dynasty loop LIVE by continuing the Marchetti save, which is already POST-SUCCESSION (gen-2 heir "Leo Sato" active at Academy age 10, 4 titles, 12 seasons managed, "🌳 The Line Continues" unlocked). Inspected the live heir hub + Trophy Room, and CODE-AUDITED the whole marquee flow (main.ts retireStar/bringThroughHeir/renderHandoff, api.ts succeed/reborn, tokens.ts rebornFields/graduatedFields/legendCardOf, narrate.ts graduationEpilogue/chapterRecap, career.ts naming). CONFIRMED-HELD from prior passes: PT-17 (handoff clear), PT-19 (send-off honours both careers), PT-27 (final-season tier move carried into send-off), and the WILL screen (🧠 Craft / 👑 Name / 💰 Fortune) each shows a blurb + maps to a real effect. No console errors. Two LIVE saves cross-checked (Marchetti→Leo Sato, Okonkwo→Jude Cruz) both show the same club-name/player-surname split. Verdict on the payoff: the STRUCTURE holds (handoff→retire→will→heir→hub swap all fire), but the EMOTIONAL/IDENTITY layer is undercut — the family name you chose isn't the name your player actually carries (PT-55), the send-off prose is flatter than the mid-career graduation (PT-57), and one succession sub-choice is a hidden/blind fake-choice (PT-56). Deduped against PT-1..54 + M1–M9. -->

- [ ] **PT-55 (stage: player late-career/heir re-pass): [high] the dynasty's family name is SPLIT IN TWO — the club/bloodline uses the name you typed at New Game ("Marchetti"), but your actual player-character + every heir carries a random surname ("Sato"), and the succession prose keys off the random one.** LIVE on the Marchetti save the hub reads "Marchetti's Club · the Marchetti bloodline" up top while YOUR PLAYER is "Leo Sato · gen 1 · pedigree 70%"; cross-checked on the Okonkwo save ("Okonkwo's Club" but player "Jude Cruz"). Root cause: New Game stores the typed name only as the CLUB name (`client/src/api.ts:209`, `register`), while the scouted prospect/founder token is named `nameFor(seed)` — a random FIRST+LAST bank pick, independent of the club (`client/src/api.ts:179,253`; `shared/src/tokens.ts:29-31`); `rebornFields` then carries that RANDOM surname down the generations (`shared/src/tokens.ts:392-396`). So `retireStar` computes the succession surname from `m.starName` → the epilogue literally says "But the **Sato** name isn't done — his son is already coming through the youth ranks" directly beneath "Marchetti's Club" (`client/src/main.ts:1696,1708`). why it hurts: the entire pitch is "found a bloodline — the family name carries down the generations", and it's the one thing the player is asked to choose at New Game (PT-39/40/41), yet the name they choose is NEVER the name their footballing family actually bears; the payoff beat celebrates a surname the player never picked and doesn't recognise · fix: either name the founder prospect after the chosen bloodline name (surname = the New-Game name, random first name from the bank) so club + player + heirs all share one family name, or let the player name the founding PLAYER (not just the club) and derive the club label from that. Ref: `client/src/api.ts:179,209,253`, `shared/src/tokens.ts:29-31,392-396`, `client/src/main.ts:1696,1708` (live: Marchetti hub + Okonkwo hub).
- [ ] **PT-56 (stage: player late-career/heir re-pass): [medium] (audit-based) the "What does he do next?" succession sub-choice is BLIND and a hidden fake-choice — the three cards render with no description until AFTER you commit, and only "mentor the heir" applies a mechanical bonus (coaching/media silently give the heir nothing).** In `retireStar` the NEXT_LIFE cards are drawn as icon+label ONLY — `<div class="cg-cname">${nl.icon} ${nl.label}</div>` with no `cg-cdesc` (`client/src/main.ts:1710-1713`) — so the evocative blurbs ("A studio wants him behind a microphone next season…") appear only once you've chosen (`:1724`); yet the very next screen, THE WILL, shows every card's blurb up front (`:1722`). Worse, only `mentoring` banks the veteran-years bonus: `appliedMentorship = choice === 'mentoring' ? mentorship : 0` (`:1717`), and the "🎓 mentored heir (+N mentality)" reward line only prints for that pick (`:1718`) — a player who picks Coaching or Media forfeits a real composure/leadership head-start for their heir (`client/src/api.ts:445`) with zero on-screen signal that Mentor was the "help your heir" option. why it hurts: at the game's most emotional decision the player chooses blind between three identical-looking cards, and the "wrong" two quietly cost the next generation a development bonus — the same hidden-tradeoff pattern flagged for contracts (PT-32/33), here at the marquee succession beat · fix: render each NEXT_LIFE card's blurb up front like the WILL cards, and surface the heir consequence on the cards ("🧭 Mentor — the only path that passes his experience to the heir") so the choice is informed. Ref: `client/src/main.ts:1666-1669,1710-1718`, `client/src/api.ts:445`.
- [ ] **PT-57 (stage: player late-career/heir re-pass): [medium] (audit-based) the RETIREMENT send-off — the dynasty's emotional peak — is a single flat template with no personality/cast flavour and zero run-to-run variation, so it reads MORE generic than the lesser age-25 graduation epilogue.** `retireStar`'s `storyLine` is one hardcoded sentence with interpolated names/counts only ("… once the heartbeat of this side on the pitch, latterly the man in the dugout — retires a club great after N seasons…"), no `pickFrom`, no persona line, no `careerCast` gaffer/mentor/rival callbacks (`client/src/main.ts:1703-1708`). By contrast the AGE-25 `graduationEpilogue` — a mid-career beat — has multi-variant open/close banks, a per-personality closing line (14 variants: "Nobody outworked him. Nobody ever will." etc.) and cast callbacks (`shared/src/narrate.ts:781-816`). So the same player's FINAL farewell is less characterful and identical every save, while his graduation years earlier was bespoke. why it hurts: the retirement/succession is the whole "bloodline dynasty" payoff and the one moment meant to feel most personal, yet it's the most template-y prose in the career — and a returning player who has graduated several bloodlines will have read the exact same retirement words every time · fix: give retirement its own `retirementEpilogue()` in narrate.ts with the same treatment as `graduationEpilogue` (personality line keyed off `t.personality`, cast callbacks, seeded multi-variant open/close) and pull his actual playing legacy (peak_overall / caps / legend rating) into it. Ref: `client/src/main.ts:1690-1708`, `shared/src/narrate.ts:779-816`.
- [ ] **PT-58 (stage: player late-career/heir re-pass): [low-med] the succession copy calls the heir "his son … already coming through the youth ranks", but the heir is actually a brand-new age-10 Grassroots prospect you develop from scratch — and "son" hardcodes gender.** The send-off says "But the {surname} name isn't done — **his son is already coming through the youth ranks**" (`client/src/main.ts:1708`), yet `rebornFields` produces a fresh gen+1 PROSPECT with `career_seed: null` / `state: 'prospect'` (`shared/src/tokens.ts:396-404`) — LIVE the heir shows on the hub as "Leo Sato · gen 1 · **age 10, ready to develop**" at Academy, i.e. a 10-year-old at the very START of the 10→25 career, not someone "coming through the youth ranks" (a later chapter). why it hurts: it over-sells where the heir is (the player expects a near-ready youth-teamer and instead gets a kid to raise from Grassroots), and the fixed "his son" closes off ever having a daughter in a family life-sim · fix: reword to match the real state ("a new {surname} is about to kick a ball for the first time" / "the next of the line is starting out"), and neutralise the gendered "son" (e.g. "the next of the {surname} line"). Ref: `client/src/main.ts:1708`, `shared/src/tokens.ts:396-404` (live: Marchetti heir hub).
- [ ] **PT-59 (stage: player late-career/heir re-pass): [low] a save that completed its succession BEFORE the PT-18 legend-snapshot fix is left permanently self-contradicting, with no backfill — distinct from PT-18 (the fix itself).** LIVE the Marchetti save has fully succeeded (gen-2 heir active, "🌳 The Line Continues" UNLOCKED) yet the Trophy Room reads "🌳 0 bloodlines · ⭐ 0 legends", the BLOODLINES panel shows the empty-state ("No bloodlines yet — develop a player… retire him to found a dynasty"), and "🔒 A Legend Retires" sits LOCKED directly beside the unlocked "🌳 The Line Continues" — the exact contradiction PT-18 fixed for NEW successions. Because `saveLegacy` only runs at `api.succeed` time (`client/src/api.ts:434-437`), pre-fix saves whose founder already retired never get a legend node and there's no migration to reconstruct one. why it hurts: existing dynasties (the most-invested players) keep a visibly broken Hall of Legends / Bloodline Tree forever — the game's signature generational surface stays empty for the people furthest into it · fix: on load (or on entering the Trophy Room), if a token is `state:'prospect'` with `generation >= 1` and no legend node exists for its prior generations, backfill a legend snapshot from the retained achievement fields so the tree/hall reflect successions that predate the fix. Ref: `client/src/api.ts:431-437` (saveLegacy at succeed only), `client/src/main.ts` showTrophyRoom (live: Marchetti Trophy Room).

<!-- Browser UX pass 2026-08-29 (player Breakthrough/senior RE-PASS — BLOCKED before senior) — continued the Delgado save (Enzo Rossi, FW ★4) from age 15 Scholar / turn 48, played the between-seasons Breakout-Year sequence (summer FOCUS spend→focus, pre-season side activity, money-vs-development "One-Club Man", mentor "The General", 4-card DRAFT) and ~5 card turns (matchdays vs Brightmoor/Dockside/Marlow + a training moment). CONFIRMED-HELD: PT-13 rival label ("you're ▲ 129 clear"), PT-44 demand legend ("green is the best match, amber also helps"), PT-16 varied money decision, PT-47/48 summer flow, draft reduce-to-1 flow, outcome text matches the card played. Then a STORY ARC fired ("🚪 THE EMPTY SEAT AT EVERY GAME") and HARD-BLOCKED all further progress (PT-52), so I could NOT reach the Breakthrough/senior stage this pass — the 🏆 League tab, 💼 Life tab, and stakes-2/3 big moments were unreachable because every story arc soft-locks the career. Root cause traced in code (single fix). No console errors. -->

- [x] **PT-52 [FIXED 521c8ea] (stage: player Breakthrough/senior re-pass): [high] story-ARC beats SOFT-LOCK the career — clicking either choice does nothing, freezing the save permanently and making the senior stage unreachable.** When the arc "🚪 THE EMPTY SEAT AT EVERY GAME" fired at Scholar (turn ~52), clicking "Agree to meet" or "Bin the letter" only applies a static amber highlight; the beat never resolves (verified across page reloads and a direct programmatic `.click()` — arc stays present, no advance, no console error). Root cause: `careerState()` returns EARLY for `st.phase === 'arc'` (`shared/src/tokens.ts:209-213`) with ONLY the arc snapshot, so the wrapper field `prospectId` is dropped; the click handler then calls `doCareerAct(s.prospectId=undefined, {type:'arc'…})` (`client/src/main.ts:2352`) → `api.careerAct(undefined,…)` throws `no such token` (caught → transient toast, no re-render). why it hurts: story arcs are a marquee senior feature and a judged item, yet the FIRST arc encountered bricks the entire save — the player cannot progress past it to Breakthrough at all; this alone blocked the whole assigned stage this pass · fix: in the arc branch of `careerState`, merge the arc payload onto the full state wrapper (include `prospectId`/`name`/`turn`/`totalTurns`) instead of returning the bare `st`, OR have `renderCareer` read `this.lastCareerState.prospectId`/the token id rather than `s.prospectId`. Ref: `shared/src/tokens.ts:206-213`, `client/src/main.ts:2352`, `client/src/api.ts:547-560`.
- [x] **PT-53 [FIXED 521c8ea] (stage: player Breakthrough/senior re-pass): [medium] during any story-ARC beat the career header blanks to "undefined" — reads "undefined · age 15" with an "undefined/undefined" turn counter.** On every render of the arc beat (fresh load included), the top bar shows literally `undefined · age 15 Scholar` and the progress pill `undefined/undefined` where the name and `turn/totalTurns` belong (elsewhere it reads "Enzo Rossi · age 15 · 52/202"). Same root cause as PT-52: the `st.phase === 'arc'` early-return in `careerState` (`shared/src/tokens.ts:209-213`) omits `name`/`turn`/`totalTurns`, and the header template consumes them raw (`client/src/main.ts:2228`: `${s.name} · age ${s.age}` and `${s.turn}/${s.totalTurns}`). why it hurts: even once the soft-lock is fixed, a story arc — the most cinematic beat — would still render the player's own name as "undefined", breaking the fiction at the emotional peak · fix: same fix as PT-52 (carry the wrapper fields into the arc state); no-name fallback in the header template as a belt-and-braces guard. Ref: `shared/src/tokens.ts:206-213`, `client/src/main.ts:2228`.
- [x] **PT-54 [FIXED] (stage: player Breakthrough/senior re-pass): [low-med] the "Breakout Year" season-event opener phrase repeats across BOTH scenario and outcome text — "Suddenly the name on everyone's lips" appeared in 3 of ~5 consecutive beats, "Riding the wave of a breakout season" in 2.** In a 5-turn Breakout-Year stretch: "Suddenly the name on everyone's lips" opened the Matchday-11 scenario, a matchday OUTCOME ("…in a rain-lashed youth-league tie, he nervelessly played A Composed First Touch…"), AND the training-ground scenario; "Riding the wave of a breakout season" opened both the Dockside scenario and the Defence-Splitter outcome. So the same seasonEvent opener is drawn for scenario openers and result flavour alike, from a small pool, producing near-back-to-back repeats. why it hurts: PT-9/PT-42 hardened the general banks, but the Breakout-Year seasonEvent opener still recycles within a handful of turns (and reusing a scenario opener as an outcome opener reads as a template seam) — the "every moment is a story" hook wears thin fast at exactly the breakout beat meant to feel special · fix: give the Breakout-Year (and other seasonEvent) openers a larger pool, track recently-used lines, and don't share one opener bank between scenario prompts and outcome prose. Ref: seasonEvent opener bank in `shared/src/narrate.ts`, consumed via `scenarioStory`/outcome narration.

<!-- Browser UX pass 2026-08-28 (player Youth/Scholar mid-career RE-PASS) — continued the Delgado save (Enzo Rossi, FW ★4) from age 11 Grassroots; played ~34 card turns forward to age 15 Scholar (turn 48/202), through a full between-seasons sequence at age 13 (SUMMER FOCUS → money-vs-development → mentor pick → 5-card DRAFT) and into a second SUMMER FOCUS at age 15 (Scholar, "Breakout Year"). CONFIRMED-HELD fixes: PT-8 (draft now explains "Rarity = power: rare and epic cards develop him more when they fit the moment. Cards you don't pick are discarded"), PT-10 (summer legend row present, e.g. "⚡ energy · 🧑‍🏫 Coach · 🏠 Parents · 👥 Teammates · 🎒 School" at age 13; trait-training tiles clearly show "🧩 train teamwork"), PT-16 (the money-vs-development screen now varies by chapter — "The Loyalty Bonus / A Magazine Cover / Back the Project" at age 13, distinct from the earlier "Big-Money Move" set), PT-43 (LIFE EVENT turns now offer off-pitch response cards), PT-45 (rival ticker rotates — "picked up an injury", "handed a longer academy deal", "captained his age group", "fast-tracked up an age group", "caught the eye of a bigger club's scouts"; occasional repeat over 6+ turns but no per-turn freeze), PT-13 (rival gap labelled "you're ▲ N clear"). The DRAFT (pick-3-of-5, reduces 3→2→1 with per-pick confirm flavour) and drafted cards later appearing in-hand (Target Man RARE, A Composed First Touch, Demand the Ball) both work. Content is well age-tailored at Scholar (agent, driving lessons, digs, private tutor, studies). CAVEATS (not filed as numbered items): (1) ENV — a concurrent editor is actively rewriting the storyarc content mid-pass: the app HMR-reloaded to the title screen every ~10-30s (save persisted, NO console errors), and twice threw a full-screen Vite build overlay — "[plugin:vite:esbuild] Transform failed … shared/src/storyarcs/offpitch.ts:461: Expected '}' but found '-'" and again at "shared/src/storyarcs/saga.ts:324" — both self-resolved on reload. This is an in-progress refactor caught mid-save (same pattern as the prior pass's storyarcs note), NOT a stable defect; it also means scenario/life-event PROSE is in flux, so I avoided filing prose-body findings this pass. (2) LIFE-EVENT framing bleed — several "⚡ LIFE EVENT · OFF THE PITCH" moments carry on-pitch performance prose/demands (e.g. "picked on for a bad miss … They want to see a bit of magic from him. Captain Ellery is looking his way." with a flair demand; and the matchday form-state openers "Stuck in a rut he can't explain" / "Low on confidence" prefix life events too) — flagged only as a caveat since offpitch.ts is being actively rewritten right now. Deduped against PT-1..46 and M1–M9. -->

- [x] **PT-47 [FIXED] (stage: player Youth/Scholar re-pass): [medium] the SUMMER FOCUS screen mixes repeatable "spend earnings" purchase tiles with the single summer-FOCUS tile that ends the screen — they look identical, and clicking a focus tile first advances immediately and strands your unspent coins with no warning or confirm.** The screen presents two card grids that look the same: "🌅 Between seasons … Steer your relationships" (8 focus tiles) and "💷 Treat yourself — or back the club — spend earnings (you have 1,020c)" (up to 9 purchase tiles). Observed behaviour differs completely with no signpost: clicking a SPEND tile ("A Private Tutor · 💷 500c") deducts coins in place (1,020c → 520c) and stays on the screen so you can buy more; clicking a FOCUS tile ("Rest & Recharge") immediately COMMITS and advances to the next screen. At age 13 I clicked the focus tile first (intending to spend afterwards) and was carried straight past the screen with all 304c unspent. why it hurts: a naive player who picks their relationship focus before spending silently forfeits an entire summer's earnings, and nothing tells them the two tile groups behave differently or that the focus pick is the "done" button · fix: visually separate/label the two groups (purchases vs the committing focus), and either gate the focus pick behind a confirm ("Spend more first?") or add an explicit "Continue" button so the focus tile isn't a hidden end-turn. Ref: summer-focus render in `client/src/main.ts`.
- [x] **PT-48 [FIXED] (stage: player Youth/Scholar re-pass): [medium] the Scholar SUMMER FOCUS legend omits the 🏠 Parents/Home meter and a ❤️ morale meter that its own tiles modify — no legend entry and no meter bar, so those deltas are unreadable (a regression from the age-13 legend, which listed 🏠 Parents).** At age 15 the legend row reads "⚡ energy · 🧑‍🏫 Coach · 👥 Teammates · 🎒 School · 🤝 Agent" — no 🏠 and no ❤️ — yet tiles hand out both: "📞 Call Home Every Night … 🏠+16", "🎁 Treat Your Parents … 🏠+14", "🚦 Driving Lessons … 👥+5 ❤️+4", "🛏️ Nicer Digs … ❤️+4", and the bottom meter row shows only ENERGY/COACH/TEAMMATES/SCHOOL/AGENT (no Parents, no Heart). The age-13 legend DID include "🏠 Parents", so Scholar dropped it while still using the icon. why it hurts: PT-10 added the legend to kill exactly this "icon soup", but at Scholar two live meters (Home and morale) have icons in no legend and no on-screen bar, so "🏠+16 / ❤️+4" read as undefined glyphs again · fix: include every icon a tile can affect (🏠 Parents/Home and ❤️ morale) in the legend and add their bars to the meter row for the Scholar stage. Ref: METER_ICON/METER_NAME + summer legend in `client/src/main.ts`.
- [x] **PT-49 [FIXED d7a9c47] (stage: player Youth/Scholar re-pass): [low-med] two cards with the IDENTICAL name and description appear together in the same life-event hand, distinguishable only by their tag pills.** Multiple LIFE EVENT hands (turns 27 and 28) dealt "Lean on the people around him / Not try to carry it alone / teamwork" AND "Lean on the people around him / Not try to carry it alone / teamwork composure" side by side — same title, same body, different tag sets. why it hurts: the card is the core choice object; seeing the literal same-named card twice reads as a duplicate/bug and makes the "which card matches?" decision needlessly confusing · fix: give tag-variants distinct names (or de-dupe the draw so two same-id/name cards can't be in one hand). Ref: off-pitch card pool + hand-draw in `shared/src/career.ts` / `narrate.ts`.
- [x] **PT-50 [FIXED d7a9c47] (stage: player Youth/Scholar re-pass): [low] a summer FOCUS effect doesn't match its described activity — "Study the Pros on VHS" (a solitary at-home activity) raises the 🧑‍🏫 Coach relationship by +10.** The tile reads "📹 Study the Pros on VHS — Rewinding the same clip over and over, trying to steal a touch you saw on the telly. ⚡-4 🧑‍🏫+10 · 👥+2" — watching videos alone at home logically shouldn't move your relationship with the coach (contrast the neighbouring "Impress the Coach — Extra sessions, first to arrive" → 🧑‍🏫+16, which fits). why it hurts: on a screen whose whole point is legible cause-and-effect, an effect that contradicts its flavour teaches the player to distrust the pills · fix: point VHS study at a trait/technique gain (or a smaller neutral meter) rather than the Coach relationship, or reword it as something the coach would actually notice. Ref: summer-focus option bank in `shared/src/career.ts`.
- [x] **PT-51 [FIXED d7a9c47] (stage: player Youth/Scholar re-pass): [medium] the "Dip in Form" debuff (PT-14) has effectively no perceptible recovery — its banner and "Low on confidence" scenario prefix persisted through ~18 consecutive Brilliant turns across TWO chapters and a summer Rest & Recharge, contradicting the pitch the entire time.** The banner "Dip in Form — You can feel it — the touch isn't quite there, and it's got into your head a bit." and the recolored openers ("Low on confidence, …" / "Stuck in a rut he can't explain, …") stamped every scenario from turn 22 (age 13 Academy, right after the summer) unbroken to turn 47 (age 15 Scholar entry) — through an unbroken run of "🎯 Right card / ⭐ Brilliant" grades AND a deliberately-chosen "Rest & Recharge" summer. PT-14 is marked FIXED ("a slump LIFTS as he strings good games together, +0.03/strong turn"), but at +0.03/turn the recovery is imperceptible over a normal sitting, so to the player the fix isn't present. why it hurts: the narration flatly contradicts what's happening on the pitch for a whole in-game year, turning the marquee prose monotonous and confusing at the exact stage this pass targets · fix: sharply raise the per-strong-turn recovery (and/or let a Rest summer clear most of a dip) so sustained Brilliant play visibly lifts the banner within a handful of turns; reopen/re-verify PT-14. Ref: form-momentum + dip-opener in `shared/src/career.ts` / `narrate.ts`.

<!-- Browser UX pass 2026-08-28 (player Grassroots RE-PASS) — new game "delgado" → sign Enzo Rossi (FW ★4) → agent → 6 card turns (age 10→11). CONFIRMED-HELD fixes: PT-39 (empty name blocks Start, no silent "My Club"), PT-40 (lowercase "delgado" live-previews as "Delgado's Club · the Delgado bloodline" and capitalises), PT-6 ("🎯 This calls for:" label + primary/secondary highlighted pills reads clearly), PT-4/PT-5 (agent screen reassures the first-timer, "first of the line · his heirs will inherit his pedigree" instead of bare 0%), PT-11 (career explainer "HOW HIS CAREER WORKS" re-shows on the new save). Wrong-card feedback ("✗ Wrong card / Poor") and stage-objective gating (wrong card did NOT increment 1/8) both correct. Menu + Grassroots read polished/premium; no contrast/too-small regressions spotted. TWO caveats worth flagging separately from the numbered items: (1) BUILD BREAK observed at load AND mid-session — the two "500 (Internal Server Error)" console errors were a Vite import-analysis failure: `shared/src/storyarc.ts:37-42` imports `./storyarcs/{saga,crisis,signature,relationship,triumph,offpitch}.js` but the `storyarcs/` dir did not exist, throwing a full-screen Vite overlay that made the app unbootable. It SELF-RESOLVED during the session when a concurrent editor created the six files (~22:32), after which play was clean — so this looks like an in-progress refactor caught mid-save, NOT a stable defect; confirm the committed tree ships with `shared/src/storyarcs/*.ts` present. (2) PT-9 REGRESSION — PT-9 is marked FIXED but its EXACT cited lines recurred verbatim this pass ("Homework still not done and Mum already shouting for the car…" turn 4; "a corner is about to be swung in, and the box is a scrum" turn 5), and turn 6's scenario was a verbatim recombination of fragments from turns 2/3/5 — so the flavor-pool fix is not holding for scenario BODIES; see PT-42. Deduped against PT-1..41 and M1–M9. -->

- [x] **PT-42 [FIXED 2819bf8] (stage: player Grassroots re-pass): [high] scenario BODY text is machine-assembled by comma-splicing a situational fragment into a body sentence, producing ungrammatical run-ons — and PT-9's verbatim reuse is NOT holding (reopen PT-9).** Every scenario reads as two unrelated clauses jammed with a comma: "Terrified of being dropped for Saturday after one bad training session, a wet pitch, a heavy ball, and a game turning into a battle of wills" (turn 1); "A growth spurt has left him gangly and not quite sure where his own feet are, a tactical shape drill, and the staff want it drilled right" (turn 2); "Homework still not done and Mum already shouting for the car, a senior pro has pulled him aside for a word he didn't ask for" (turn 4). Worse, turn 6's Training Ground scenario was a verbatim splice of fragments already seen — "A growth spurt has left him gangly…" (=turn 2) + "a one-on-one against the reserve-team hardman — no easy reps today" (=turn 3) + "It's about the effort nobody notices until it's missing" (=turn 5) — i.e. the exact PT-9 symptom, within 6 turns. Two-in-one root cause: the fragment inserted mid-sentence is never grammatically joined, and the fragment pool is tiny so bodies recombine. why it hurts: for a text game whose whole hook is "every moment is a story", the marquee prose reads as broken/robotic AND repetitive on a first sitting · fix: give each scenario body a single coherent authored sentence (or join the fragment with proper syntax, not a comma), enlarge the body-fragment pool, and track recently-used fragments; reopen PT-9. Refs: `shared/src/narrate.ts` scenario/body banks, `shared/src/career.ts` makeScenario.
- [x] **PT-43 [FIXED 74f82e3] (stage: player Grassroots re-pass): [medium] off-pitch LIFE EVENT moments offer only football-action cards, so the "how does he handle it?" answers are nonsensical for the situation.** The "⚡ LIFE EVENT · OFF THE PITCH" turn ("Homework still not done and Mum already shouting for the car, a senior pro has pulled him aside…") asks "How does he handle it?" and then offers Stepover ("Take your man on with a stepover and a burst of pace"), Ice in the Veins, Defence-Splitter ("Slide a defence-splitting pass through the eye of a needle") and Lung-buster Run — pure on-pitch skill moves as ways to deal with homework/Mum/a senior pro. The demand was even "flair". why it hurts: it's a direct text-doesn't-match-the-situation break at a moment meant to add life-sim texture — playing "a defence-splitting pass" to answer a life event reads as a content bug · fix: give off-pitch/life events their own response-card pool phrased as choices (how he reacts, what he says/does off the pitch), or reframe those turns so football cards make sense.
- [x] **PT-44 [FIXED c8a6f9f] (stage: player Grassroots re-pass): [medium] a demand tag shown under "🎯 This calls for:" is graded "✗ Wrong card" when you play it — the primary(green)/secondary(yellow) pill distinction is never explained.** Multi-tag demands render every tag as an equal pill after "This calls for: … — play a card that matches" (e.g. turn 3 "creativity flair"; turn 5 "stamina creativity teamwork"), with only a green-vs-yellow colour hinting that one is the real match. Playing the yellow-listed tag scores as failure: turn 3 demand "creativity/flair", I played a **flair** card (Stepover) and got "✗ Wrong card ◦ Scrappy" — flair was literally listed under "This calls for:". why it hurts: PT-6 added the label but the colour semantics are undocumented, so a naive player rationally plays a called-for tag and is told they're wrong, teaching them to distrust the demand pill · fix: explain the colour (green = the match, yellow = partial/secondary) with a legend/tooltip, and don't brand a partial-credit tag "Wrong card" when it's shown as called-for and still develops him.
- [x] **PT-45 [FIXED 74f82e3] (stage: player Grassroots re-pass): [low-med] the rival news ticker prints the identical headline on every turn of the chapter.** "📰 Turner has just been picked up by a bigger local side." appeared unchanged on all 6 turns played (turns 1–6), including after the rival gap had swung both ways. why it hurts: the rival is sold as a living parallel career to chase; a frozen one-line headline makes his "story" feel canned and undercuts the "measured against him" hook · fix: rotate the rival-news line (tie it to his actual progress/gap swings, or draw from a pool the way PT-9 intends for flavor), and don't repeat the same headline turn-to-turn. Ref: rival-news render in `client/src/main.ts` rivalHtml / `shared/src/narrate.ts`.
- [x] **PT-46 [FIXED 2819bf8] (stage: player Grassroots re-pass): [low-med] Grassroots scenarios use pro-club language that clashes with the age-10 "jumpers for goalposts" framing.** The chapter is framed as "Grassroots · Jumpers for goalposts — muddy knees and big dreams" for a 10-year-old, yet the moments talk like a senior academy: "dropped for Saturday", "make the actual team, not just the bench", "a one-on-one against the reserve-team hardman", "the staff want it drilled right", "a senior pro has pulled him aside". why it hurts: the tonal mismatch between the whimsical grassroots billing and adult reserve-team vocabulary is jarring at the very first chapter and reads as fragments written for a later stage · fix: age-gate the scenario fragment banks so Grassroots draws child/park-football language (coaches, parents, school teams) and reserves "reserve team / staff / dropped for Saturday" for the later academy/senior chapters. Ref: `shared/src/narrate.ts` scenario banks keyed by chapter.

<!-- Browser UX pass 2026-08-28 — player Grassroots (new game → sign prospect → agent → 6 card turns). No console errors. POSITIVE: skilled card play is now clearly rewarded (Right card/★Brilliant vs Wrong card/✗Poor is unmistakable), stage objective correctly counts only strong displays (wrong card did NOT increment), and the family-name live preview ("Okonkwo's Club · the Okonkwo bloodline") works well. -->

- [x] **PT-3 [FIXED — PERSONALITY is now a multi-variant, outcome-neutral bank picked at random (no more one recycled line; reads fine on win or loss). shared/src/narrate.ts.] (stage: player Grassroots): [high] outcome flavor text recycles within 6 turns — a workhorse player sees the identical voice line "No one on that pitch worked harder." on almost every strong/failed beat.** The prose sells the "every moment is a story" hook, but repetition breaks it fast: in a 6-turn run that one sentence appeared in 5 of 6 outcomes, plus "As a dog wandered across the far corner" ×3 and "Somebody, somewhere, will tell this story wrong in twenty years" ×2. Root cause: `PERSONALITY` is the only vocabulary bank that's one fixed string per id instead of an array, and it's appended ~60% of the time on triumph/dismal (`shared/src/narrate.ts:88-102`, used at `narrate.ts:321`). Fix: make `PERSONALITY` a `Record<string,string[]>` (like `PERSONALITY_ADV`) and `pick()` from it; also the same "worked harder" line reading as a compliment on a *dismal/Wrong-card* result is tonally off.
- [x] **PT-4 [FIXED — stars labelled as potential (tooltip); founding pedigree now shows 'first of the line · his heirs will inherit his pedigree' instead of a bare 0%. main.ts pedigreeText().] (stage: player Grassroots): [medium] the signed prospect shows "★★★★☆" and "pedigree 0%" side by side, which reads as a contradiction.** The academy explainer only defines pedigree ("the quality his bloodline passes down — a higher pedigree means a stronger natural ceiling"), so a new player sees 4 gold stars next to 0% and can't tell if the kid is great or worthless; the stars are never labeled or explained. Founding-generation pedigree is legitimately 0, but nothing signposts that stars = his own talent while pedigree = inherited bonus. Fix: label the stars (e.g. "talent"), and either hide "pedigree 0%" for the founding prospect or annotate it ("0% — you're the first generation; his kids inherit his pedigree").
- [x] **PT-5 [FIXED (lighter touch) — agent screen now reassures the first-timer ('no wrong pick; the tags play out slowly over the years'); kept all agents (variety) rather than trimming.] (stage: player Grassroots): [medium] the agent screen dumps 11 near-identical agents as the very first real decision, with effect pills that reference systems the player hasn't seen yet.** Choice overload before any card turn: pills like "more big-stage moments", "better card options at drafts", "higher transfer value" are meaningless to someone who has not yet played a matchday or a draft, and two 🃏 tiers ("slightly better drafts" vs "better card options at drafts") can't be ranked by a newcomer. Fix: trim the first-career agent list to ~3-4 clearly-differentiated options (or gate the rest behind later generations), and add a one-line "what this means" tooltip on the pill dimensions.
- [x] **PT-6 [FIXED — the demand is now labelled '🎯 This calls for:' with distinct highlighted pills (primary emphasised) + 'play a card that matches' hint, visually separate from card tags.] (stage: player Grassroots): [low-med] the scenario "demand" tag is a bare pill visually identical to the card tags, with no label telling the player it's the thing to match.** The moment's wanted trait (e.g. a lone "teamwork" pill, or "flair teamwork creativity") sits under the scenario prose in the same pill style as the four cards' tags, so a naive player may not realize matching that pill to a card tag is the core mechanic. It's also ambiguous when 3 demand tags appear but each card carries only 1-2 tags — which card "matches" isn't clear. Fix: prefix the demand pill(s) with a label ("They want:") and visually distinguish them from card tags; consider highlighting matching card tags.
- [x] **PT-7 [FIXED — the Player-tab role now carries a '· forming' cue (while traits still form) + a tooltip explaining position emerges from how you develop him.] (stage: player Grassroots): [low-med] a prospect pitched in the academy as "Looks like a forward" is classified DF on the Player tab after a couple of turns, with no explanation.** A new player who deliberately picks the "forward" prospect (Jude Cruz) is confused to open Player and find "DF · forming Ball-Winner · TAC 16". If position emerges from early card choices/stats that's a nice system but it's completely unsignposted, so it reads as a bug. Fix: either honor the academy descriptor as the starting position, or surface a note that his position/archetype is still forming from how you play him.
- [x] **PT-8 [FIXED — draft prompt now explains rarity = power (rare/epic develop him more when they fit) and that unpicked cards are discarded; reframed 'build your identity' as which moments he can answer. main.ts.] (stage: player Youth/Scholar): [medium] the between-seasons DRAFT sells "this is how you build your identity" but is a reject-2-of-5 pick (reject-1 in a breakout year), and the RARE/EPIC card pill is shown with zero explanation of what rarity does.** With `OFFER_SIZE=5` and `DRAFT_PICKS=3` (my draft was 4-of-5 due to a Breakout-Year bonus pick), the newcomer's only real decision is which one or two cards to cut — thin agency for a mechanic framed as identity-building — and a "RARE" card like Target Man carries a bare rarity pill (`cg-rarity`) with no tooltip, so there's no in-game signal that rarity means a bigger stat pull (it does — `shared/src/career.ts:59`). Fix: explain rarity on the card (tooltip: "rarer cards pull your stats harder"), and/or widen the offer pool so drafting is a real trade-off. Refs: `client/src/main.ts:2265-2268` (rarity pill), `:2189` ("build your identity"), `shared/src/career.ts:372-373` (OFFER_SIZE/DRAFT_PICKS).
- [x] **PT-9 [FIXED — expanded the openers (SETTINGS 6→11/chapter) and closers (REACTIONS 8→12/band) so flavour doesn't recycle within a chapter. shared/src/narrate.ts.] (stage: player Youth/Scholar): [medium] matchday flavor text still recycles verbatim within a chapter — PT-3's fix only touched the PERSONALITY bank, leaving the scenario-opener, scenario-prose and closer banks under-varied.** In a ~14-turn Grassroots run the opener "as a dog wandered across the far corner" appeared 4 times, the closer "the engine simply doesn't cut out" 4+ times, and whole scenario lines repeated word-for-word ("Homework still not done and Mum already shouting for the car…"; "A corner is about to be swung in, and the box is a scrum"). Root cause: small fixed pools relative to turns-per-chapter — only 6 Grassroots openers (`shared/src/narrate.ts:35`), fixed match/scenario banks (`narrate.ts:137`, `:182`), workhorse closers (`:95`). Fix: enlarge these pools and/or track recently-used lines to avoid near-term repeats (as PT-1's hand logic does for cards).
- [x] **PT-10 [FIXED — summer FOCUS now shows an icon legend (⚡ energy + the relationship meters in play) and each effect has a full-name tooltip. main.ts METER_NAME.] (stage: player Youth/Scholar): [low-med] the summer FOCUS screen renders each option's effects as bare icon-math (e.g. "🧑‍🏫+16 · 👥-3", "🧩 train teamwork") with no inline legend, mixing two unlabeled icon vocabularies in one pill row.** The first-timer HELP card explains relationships conceptually ("keep them healthy for better moments"), but never maps the glyphs (🧑‍🏫=coach, 🏠=family, 👥=teammates, 🎒=school) or distinguishes relationship-meter deltas from trait-training ("train teamwork"), so eight tiles of "+16/-3" icons still read as soup on first encounter. Fix: label the pills or add hover text on each glyph; visually separate "trains a trait" from "changes a relationship". Refs: `client/src/main.ts:2201` (effLabel), `:2217`, METER_ICON `:39` / TAG_ICON `:50`.
- [x] **PT-11 [FIXED — onboarding flags (fm_tut_done / fm_career_help_done) now keyed per-save (onbKey), so a new bloodline/New Game re-shows the onboarding instead of a global flag suppressing it forever. main.ts.] (stage: player Youth/Scholar): [low] the first-timer onboarding (the "HOW HIS CAREER WORKS" help card + tutorial coach-marks) is gated on GLOBAL localStorage flags, not per-save, so a returning browser never re-shows it.** `fm_career_help_done` / `fm_tut_done` are set once and persist across saves (`client/src/main.ts:2037,2240`); starting a brand-new bloodline/New Game does not reset them, so a player who samples the game once and later starts their "real" first dynasty gets no career explainer — and it silently suppressed the onboarding for this browser playtest until the flags were cleared by hand. Fix: reset these flags on New Game (or scope them per-save) so every genuinely-new career shows the explainer once.

<!-- Browser UX pass 2026-08-28 (senior stages) — new game → played ~90 turns from age 10 Grassroots to age 19 Breakthrough (turn 110/202). Reached & confirmed the senior content: BIG GAME / ★ THE BIG ONE matchdays, 🔥 vs Turner rivalry + 🔥 SHOCK CALL-UP variants, the 🏆 League tab (age 17) and 💼 Life tab (age 19). POSITIVE: the League tab is excellent — "Breaking in · 5/18 apps", "Marchetti's Club sit 4th of 10 · 8W 6D 4L · 30 pts", a full 10-team table (P/W/D/L/GF/GA/GD/Pts) with the club highlighted, and the explicit link "the more he features and the better he plays, the higher his club climbs." The Life tab is also strong and worthwhile — Public Image ("Global icon"), 4 endorsements that convert fame→coins each with an image-constraint caveat, 10 collectible signature boots, and a clear progression hook ("🔒 Next: The Last Word — Career score 1200+ (937/1200)"). The special-matchday headers DO escalate visually (star/fire icons, distinct subtitles, a different intro prompt e.g. "Thrown in cold, hours to think about it"). Also GOOD: "Right card / Unlucky" correctly decouples decision-quality from variance — a well-matched 2-tag card that rolls low still shows the reassuring "Right card" chip + hedged flavor ("half-worked, promising"), so high-stakes variance reads fine. -->

- [x] **PT-12 [FIXED — big games now PLAY bigger, not just look it: stakes-2 moments demand ≥2 tags (stakes-3, three) so they test the deck instead of a single-tag gimme, AND get a distinct 'big occasion' payoff beat in the narration (stakes-3 already had the HUGE sequence). shared/src/career.ts + narrate.ts.] (stage: player Breakthrough/senior): [medium] big-stakes moments look escalated but play the same or EASIER than routine turns, and the outcome never acknowledges the stakes.** BIG GAME / ★ THE BIG ONE / 🔥 vs Turner / 🔥 SHOCK CALL-UP get distinct headers, borders and icons up front, but the card decision on them is frequently a SINGLE-tag demand (e.g. ★ THE BIG ONE vs Turner asked only for "flair"/"composure"), which is *easier* to satisfy than a routine 3-tag turn — so the mechanic gets looser exactly when the framing says it should tighten. The resolution then reuses the identical "Right card / Brilliant" chip and the same closer bank ("…the engine simply doesn't cut out. One in the eye for Turner.") as a meaningless reserve friendly. Net: tension is entirely front-loaded in the header; the choice and the payoff don't ratchet, so a naive player quickly learns the "BIG" tags are cosmetic. Fix: on big/★ matchdays raise demand complexity (more tags / tighter hand) and add stakes-aware outcome flavor + a visible reward (fame/boot/league swing) so the payoff matches the billing.
- [x] **PT-13 [FIXED (labeling, not balance) — the gap is YOUR score minus his; it now reads 'you're ▲ N clear' / 'you're ▼ N behind — reel him in' so it's unambiguous who leads. With good play you correctly pull clear. main.ts rivalHtml.] (stage: player Breakthrough/senior): [medium] the rival (Turner) gap balloons out of reach regardless of near-perfect play, contradicting the onboarding promise.** Onboarding sells the rival as beatable: "you're measured against him — out-develop him to pull ahead." Across a run graded ~90% Brilliant, the "ahead" counter went 74 → 157 → 313 → 497 (roughly doubling each season) while Turner's story escalated independently (first-team, "linked with a move across the city"). The gap never responded to my strong grades, so the rival comparison reads as cosmetic and unwinnable — demoralizing and directly at odds with the tutorial. Fix: tie the rival's rate to the player's actual performance so sustained strong play visibly closes the gap (and can overtake), making the "out-develop him" promise real.
- [x] **PT-14 [FIXED — form is now momentum: a slump LIFTS as he strings good games together (+0.03/strong turn) and its banner retires to 'Back to Form'; purple patches cool if results dip. No more 40-turn 'Low on confidence' stamp. shared/src/career.ts.] (stage: player Breakthrough/senior): [medium] the chapter-long "Loss of Form / Dip in Form" debuff has no in-chapter recovery and prefixes ~40 consecutive scenarios with "Low on confidence" despite unbroken success.** The banner ("Dip in Form — the touch isn't quite there…") persisted the ENTIRE Scholar chapter (~40 turns) through an unbroken streak of Brilliant results AND a summer "Rest & Recharge", only clearing at the chapter boundary — then re-triggered at the start of Youth Team and again at Breakthrough. Every scenario opener is recolored "Low on confidence, …", so the narration flatly contradicts what's happening on the pitch and turns monotonous. Distinct from the PT-9 flavor-pool work: this is a debuff-mechanic problem (no recovery path, wrong-signal framing), not bank size. Fix: let sustained strong play or a Rest summer visibly reduce/clear the dip, and stop stamping "Low on confidence" on turns the player is winning.
- [x] **PT-15 [FIXED — match-flavoured moment names (Derby Day/Cup Final) are now gated to MATCH scenarios only (rng draw preserved), so they no longer bleed onto training/life screens. shared/src/career.ts makeScenario.] (stage: player Breakthrough/senior): [low-med] match-only context tags bleed onto TRAINING GROUND and LIFE EVENT screens, where there is no match.** Repeatedly saw match-fixture phrases on non-match scenarios, e.g. "⚔ TRAINING GROUND … it's Derby Day", "⚔ TRAINING GROUND … it's Relegation Six-Pointer", "⚔ TRAINING GROUND … it's Cup Quarter-Final", and "LIFE EVENT … it's The Return to a Former Club". Describing a closed-doors drill or an off-pitch life moment as a "Derby Day"/"Cup Quarter-Final" is immersion-breaking and reads like a content bug. Fix: gate the match-context phrase bank to actual MATCHDAY scenarios; give training/life events their own context lines.
- [x] **PT-16 [FIXED — the between-season money decision is no longer identical every year: each archetype (money/fame/develop) now has 4 concrete variants, and rollOffer picks a different one per chapter (ids + character stable for replay safety). shared/src/career.ts.] (stage: player Breakthrough/senior): [low] the between-season "money now vs development" decision is the identical 3-option screen every single season.** Big-Money Move (+900c) / Boot & Kit Deal (+500c) / Stay & Develop (+120c) appears verbatim — same three options, same body text, same effect pills — at every chapter break (seen 4+ times: Academy → Scholar → Youth → Breakthrough). For a recurring headline choice framed as character-defining, zero variation plus no visible cumulative consequence (beyond the one-off coins) makes it read as filler by the second time. Fix: vary the offers/wording by chapter/fame level and surface how repeated greedy-vs-loyal picks compound over the career (personality, Fans meter, pedigree, or the Life-tab endorsements they clearly feed).

<!-- Browser UX pass 2026-08-28 (dynasty loop) — continued the Marchetti save from age 19 Breakthrough, played card turns to the HANDOFF (age 22, "take the reins"), then managed 11 seasons (sim-the-rest each year) to Sam Sato's RETIREMENT at 34 → SUCCESSION (will/inheritance) → heir Leo Sato (gen 1). No console errors anywhere. POSITIVE: all three signature beats land well. HANDOFF is clear and exciting ("his career becomes your club's story — you pick the XI, set the tactics"). RETIREMENT is a legible send-off ("hangs up his boots after 11 seasons… a club great") with a nice "what does Sam do next?" choice (coaching / media / mentor the heir). SUCCESSION is strong: "the Sato name isn't done — his son is already coming through the youth ranks", a clear will choice (🧠 The Craft / 👑 The Name / 💰 The Fortune, each with a distinct mechanical effect), the heir revealed as "🌱 Leo Sato · gen 1 · pedigree 70%" (a real inherited jump from the founder's 0% — The Name adds pedigree), the "🌳 The Line Continues" achievement fires, and the hub swaps to the heir as the active bloodline player with the manager title bumped to "Promising Boss". The generational NAME carries correctly (Sam Sato → Leo Sato). Also useful: "⏩ Sim the rest of the season" makes the multi-season climb painless, and the match "Skip ▶▶" is instant. (Note: the desktop match view painted black in this headless pane — the DOM/state were all correct, so this is an environment paint quirk, NOT a game bug.) -->

- [x] **PT-17 [FIXED — taking the reins now auto-picks a fresh XI from the merged squad and force-includes the bloodline star (drops the weakest starter if needed), so he's never benched at handoff. main.ts renderHandoff.] (stage: handoff): [medium] the moment after the handoff, the default match lineup BENCHES the very star the handoff just promised.** The take-the-reins card says "Sam Sato your man on the pitch", but the first SET LINEUP opens with an all-local XI (ratings 6–9; Key player shown as "Luis Santos (GK, OVR 9)") and Sam Sato (DF 16 💎, by far the best player) not selected in any of the 11 slots — you only get him in by clicking "Auto-pick best XI" or hand-adding him. A naive player can kick off their first-ever managed match with their bloodline star sat on the bench, directly undercutting the emotional beat. Root cause: `openLineup('match', …)` fills the draft XI from stale/default standing orders (soValid path) that predate the star joining the club, instead of guaranteeing the managed star is in the XI. Fix: force-include the managed star (or fall back to `autoPickXI`) whenever the standing XI omits him. Ref: `client/src/main.ts` openLineup() ~2571–2611.
- [x] **PT-18 [FIXED (HIGH) — succeed() snapshots the retiring pro as a legend (legendCardOf + saveLegacy, :g<gen> keyed) before reborn, so the Bloodline Tree / Hall of Legends populate. Regression-guarded in qa_offline_facade. client/src/api.ts.] (stage: succession): [high] the signature Bloodline Tree never populates — after a full retire→heir succession the Trophy Room still reads "No bloodlines yet · 🌳 0 bloodlines · ⭐ 0 legends".** I completed the whole loop (Sam Sato retired a 3-title club great; heir Leo Sato born, gen 1, pedigree 70%; "The Line Continues" achievement UNLOCKED; hub now shows the gen-1 heir), yet the Trophy Room's BLOODLINES section shows the empty-state ("develop a player… retire him to found a dynasty. Every generation after adds a link to the tree") and the "A Legend Retires" achievement stays 🔒 locked — sitting right next to the just-earned "🌳 The Line Continues", a visible self-contradiction. Root cause: `showTrophyRoom()` builds the tree from `api.legends()` → `legaciesFor()`, but the only writer to that store, `saveLegacy()` (`client/src/save.ts:216`), and `retirePlayer()` (`save.ts:195`) are DEFINED BUT NEVER CALLED anywhere in `client/src`/`shared/src` — so the manager-phase retirement/`api.succeed()` never records the founder as a legend or marks the token retired. The game's whole premise (see a bloodline "across the generations") has no payoff surface. Fix: on star retirement in `bringThroughHeir()`/`api.succeed()`, call `saveLegacy()` (and `retirePlayer()`) so the founder becomes a legend node and the Sam → Leo chain renders. Refs: `client/src/main.ts:1886` (showTrophyRoom), `client/src/api.ts:585` (legends), `client/src/save.ts:195,216`.
- [x] **PT-19 [FIXED — the retirement send-off now honours both careers ('once the heartbeat on the pitch, latterly the man in the dugout … two careers in the same shirt'). main.ts retireStar.] (stage: retirement): [low-med] the send-off honours only the MANAGERIAL record and forgets the playing career you actually played.** The retirement card frames it as the player hanging up his boots ("Sam Sato hangs up his boots") but credits only "11 seasons steering Marchetti's Club and 3 league titles" — the club's managerial honours. Nothing recaps HIS career: the years of card-turns you played from age 10, his 30+ international caps, POTM moments, or on-pitch grades. For a bloodline life-sim whose emotional weight comes from having lived his whole playing life, a farewell that only tallies the club's trophies feels impersonal at the exact beat it should feel most personal. Fix: lead the send-off with his playing legacy (career apps/goals/caps/POTM/legend rating) and treat the managerial titles as a secondary line.

<!-- Browser UX pass 2026-08-28 (manager first season) — GOAL: play a first managed season (season screen, XI/tactics, play fixtures, table, end-of-season). REACHABILITY BLOCKER (see PT-23): none of the 6 saves is past the handoff — all are pre-handoff prospects (Marchetti already went the whole way and is back at Academy with heir Leo, gen 1). Handoff is hard-gated at ~turn 110 / entering Breakthrough (age 19) and needs a full first-team Youth season (firstTeamReady bandIdx≥3 && apps≥11, clubseason.ts:21 + tokens.ts:274). Furthest save (Rivera / Sam Marin) is turn ~39 — ~70 uninterrupted card turns from the season UI, with no resume-into-season or skip-to-handoff, and the one code shortcut is dead (hub-season-dev → showSeason, main.ts:678, element never rendered). Two NON-DESTRUCTIVE attempts to seed a manager state to reach the season screen were BLOCKED by the environment classifier. So PT-20..22 are CODE-GROUNDED findings from auditing the actual manager-phase render/logic (showSeason, openLineup, renderLineupEditor, renderMatchReport, clubLeagueStrength, autoPickXI) rather than live-observed. Positives from the audit: showSeason is rich and legible (fixtures with derby tags, live table with the club highlighted, form strip, records, board verdict, sponsor/staff/transfer hooks, ⏩ sim-the-rest); the star DOES mechanically drive the table (top-11 avg incl. him + his age modifies clubLeagueStrength, main.ts:1075); there's a real pre-match TEAM TALK with personality modulation (main.ts:1719). No console errors on any screen reached (menu/hub/facilities/career). -->

- [ ] **PT-20 (stage: manager first season): [medium] PT-17's "star never benched" fix is handoff-only — `autoPickXI` has no star guard, so changing formation in the lineup editor (or stale standing orders) can silently re-bench the bloodline star.** PT-17 force-includes the star only inside `takeReins` (`client/src/main.ts:2128-2133`). But the lineup editor rebuilds the XI with a bare `autoPickXI(this.availableClub(), formation)` on every formation change (`main.ts:2662`) and as the fallback when standing orders are invalid (`main.ts:2594`), and `autoPickXI` (`shared/src/teams.ts`) just fills slots by role+overall with zero bloodline/star concept. A young heir (early generation, lower overall than bought-in squad players) or a GK/DF star can therefore drop out of the XI the instant the manager tweaks the formation — the exact emotional-beat regression PT-17 was meant to kill, reappearing one screen later. Fix: move the force-include into `autoPickXI` (or a shared helper both paths call) so the managed star is guaranteed in the XI everywhere, not just at handoff.
- [x] **PT-21 [FIXED — the season screen now shows the star's OVR with a tooltip linking his rating to the club's finish; the FT report spotlights the star when he scores and biases POTM toward him. main.ts.] (stage: manager first season): [medium] the star's performance→club result link is mechanically real but invisible — no per-season star stats on the season screen, and the full-time report never flags him as "your man".** The dynasty's core promise is "his displays drive the club," yet: (a) the season screen header only prints "★ {name} (age N, likely retires ~M)" — no apps/goals/rating/form for the star this season (`showSeason`, `main.ts:1183`); and (b) the post-match report builds scorers/assists/Player-of-the-Match purely from goal events with no notion of the bloodline player — POTM is "most goals, tie-broken to the winning side" and can be an OPPONENT, and the star is just another name in the scorer list if he happened to score (`renderMatchReport`, `main.ts:2894-2929`). So a naive player has no on-screen thread connecting "how my guy played" to "how the club did," even though his overall+age silently feed `clubLeagueStrength`. Fix: give the star a match rating + a season stat line (apps/goals/assists/POTM) surfaced on the season screen and called out on the FT card ("★ Sam Marin — 8.4, 1 goal · your man ran it").
- [x] **PT-22 [FIXED — removed user-facing 'NFT'/'Owned NFT' from squad/lineup/bench tooltips ('Your star'/'Bloodline star'); remaining 'nft' is code/CSS only. main.ts.] (stage: manager first season): [low-med] "NFT" terminology leaks into the manager-phase UI (lineup, bench, squad table, player card) even though web3/NFT was deliberately removed from the game.** Hovering the bloodline star in the SET LINEUP shows title="NFT star · {tier} tier — click to view card" (`main.ts:2705`); the bench and squad-table tooltips read "Owned NFT · {tier}" (`main.ts:2750`, `:276`); and the collectible player card / lifecycle panel are internally framed as "NFT" throughout (`main.ts:697,806,838`, plus `isNftId`/`nftTier`). For a premium, offline, bloodline-DYNASTY life-sim that removed web3, seeing "NFT" on your own family star breaks the fiction at a signature moment. Fix: rename the user-facing strings (and ideally the `nft*` helpers) to bloodline/dynasty language ("bloodline star · {tier}") — the visible tier icon+name are fine; it's the "NFT"/"Owned NFT" tooltips that leak.
- [x] **PT-23 [FIXED — added a one-time, per-save 'YOU'RE THE MANAGER NOW' explainer on the season screen (set XI+tactics → play/sim → climb the table; the star is your key player; spend on transfers/facilities). main.ts managerHelpCard.] (stage: manager first season): [low-med] the first-ever managed lineup screen dumps the full tactics suite on a player whose entire prior UI was "pick 1 of 4 cards," with no first-manager onboarding.** The handoff sells it gently ("you pick the XI, set the tactics"), but SET LINEUP then presents, at once: formation + ~6 tactic dropdowns (mentality/line/width/tempo/pressing/…) + Offside Trap + Play-Out-From-Back toggles + Attack Focus + 11 player dropdowns + 11 duty dropdowns + captain/pen/FK/corner badges + a conditional MATCH PLAN panel + (at kickoff) a TEAM TALK choice (`renderLineupEditor` `main.ts:2647-2688`). The card career gets a "HOW HIS CAREER WORKS" explainer and tutorial coach-marks (PT-11); the manager transition gets none, and the only guidance is `title=` tooltips (undiscoverable, useless on touch). A fresh manager faces choice-overload exactly at the stage handoff — the mirror of the card-side PT-5/PT-10 overload. Fix: add a one-time first-manager coach-mark/explainer for the lineup+tactics screen (what a duty is, what "auto-pick" does, that tactics are optional), and consider progressive disclosure (collapse advanced tactics behind an "Advanced" toggle for season 1).

<!-- Browser UX pass 2026-08-28 (manager promotion/relegation climb) — LIVE. Reached the manager phase by continuing the Marchetti save (tier 5 / National League, gen-1 heir, 3 titles, 18,721c) and seeding the exact season state the hand-off itself creates (season 1, star on the pitch) so the real "Continue the season" flow renders — then played it through the shipped UI: simmed a full 18-game season (finished 4th/10, no move), rolled over to season 2 (tier persisted, board verdict shown, star aged), forced a champions finish, and watched the promotion reveal + the new tier fire. No console errors on any screen. POSITIVES (live): the promotion reveal is clear and satisfying ("⬆️ PROMOTED — welcome to League Two. The football gets harder from here — this is the reward for the climb."), the season header + table name the tier as a real climb ("the National League" → "League Two"), the table's promotion/relegation colour key is CONSISTENT with the actual rule (top-2 up / bottom-2 down — no off-by-one; the display zones in spTableHtml match nextSeason's promoted/relegated test exactly), bounds are guarded (no promote above tier 1, no relegate below tier 10), the tier is a club property that correctly persists across the season rollover, and each division re-seeds a fresh, mostly-distinct opponent set + derby rival. PT-24/25 are live-observed; PT-26/27/28 are code-grounded (audit-based). -->

- [x] **PT-24 [FIXED — liveTable now takes a per-season resultSeed (roster stays fixed to leagueSeed = your division's clubs, but the OTHER clubs' results vary each season), so seasons no longer replay byte-for-byte. shared/src/clubseason.ts + main.ts seasonResultSeed().] (stage: manager promotion/relegation): [medium] the league is FROZEN across seasons at a tier — same opponents, same fixture order, same rival results every year — so the table has no life of its own.** `leagueSeed()` is derived only from the account handle and never changes, and `seasonFixtures`/`liveTable` take no season argument, so a save replays the identical 9 opponents in the identical order every season at a tier, and the opponent-vs-opponent scorelines are byte-identical too (`simMatch` is seeded `hash32(seed, r*53+hi*7+ai)` with no season term — `shared/src/clubseason.ts` liveTable/simMatch). Confirmed live: National League seasons 1 and 2 were pixel-for-pixel the same fixture list. Rivals never have their own good or bad campaigns, the surrounding table is fully deterministic, and a mid-strength club that plateaus finishes in the exact same slot forever. It reads as a static ladder, not a living league, and makes multi-season stays at a tier feel like re-watching a recording. Fix: fold the season number into the opponent draw + match seeds (e.g. `hash32(seed, tier, season, …)` in `seededOpponents`/`liveTable`/`simMatch`) and/or jitter opponent strengths a little per season so the table around you actually moves.
- [~] **PT-25 [MONITOR (likely test artifact) — observed with a seeded star of OVR '—' (broken strength); the manager probe with a real strength arc shows a healthy, non-yo-yo climb (careers end tier ~1.0). Re-open if a cliff reproduces with a real star.] (stage: manager promotion/relegation): [medium] a single promotion can be a difficulty CLIFF, not a ramp — pushing a mid-table side straight to the bottom and back down (yo-yo).** With the club's strength held ~constant across a promotion, a side that finished 4th/10 in the National League (tier 5) finished DEAD LAST, 0 wins / 8 draws / 10 losses, in League Two (tier 4) the very next season — i.e. promoted then immediately relegation-bound. The per-tier baseline only steps by 1 (`tierStrength = 15 - tier`, `shared/src/clubseason.ts`), but the manager-sim match model is very low-scoring (whole seasons of 1-0 / 0-0 / 0-1), so a 1-point strength deficit flips a pile of narrow wins into draws/losses and swings the table violently. Combined with the frozen league (PT-24), a plateaued club can bounce between two tiers deterministically with no way to break out except external coin investment. (Caveat: this live run used a placeholder star with OVR "—"; a real developing bloodline star would lift squad strength — but the adjacent-tier transition still reads as a wall rather than a step.) Fix: soften the per-tier baseline step or widen match goal variance so a newly-promoted side is competitive-but-stretched rather than winless; consider a one-season "settling"/parachute cushion after a move.
- [~] **PT-26 [ACCEPTED — the probe uses seasonTable (full round-robin) as a balance approximation, not the game's fills-as-you-play liveTable; it measures balance correctly and PT-24 now makes the real league vary per season too. Not worth converging the two models.] (stage: manager promotion/relegation): [low] (audit-based) the balance probe `analyze_manager_career.ts` doesn't model the shipped league, so its green verdict is optimistic.** The probe reseeds every season (`seed = ((i + 1) * 2654435761) ^ (s * 40503)`, `tools/playtest/analyze_manager_career.ts:32`) and uses `seasonTable` (an independent full round-robin), whereas the live game uses a CONSTANT `leagueSeed()` and the incremental `liveTable` path (per PT-24). So its "100% reach the top / 0% stuck" numbers are measured on a far more forgiving, per-season-varying league than players actually get. It also assumes a monotonic strength ramp 7→16 (`strengthAt`, `:20`) that ignores the per-generation reset when a young heir inherits the club at a lower overall. Net: the probe can bless a climb that, in the real deterministic league, plateaus or yo-yos (PT-25). Fix: point the probe at the game's constant-seed `liveTable` path and model the hand-off strength dip so the harness measures the league players actually climb.
- [x] **PT-27 [FIXED — a promotion/relegation in the star's final season is now carried into the retirement send-off ('in his very last season he took the club up to League Two …'); the reveal banner would be stale for the heir a generation later, so it's acknowledged at the moment instead. main.ts retireStar.] (stage: manager promotion/relegation): [low] (audit-based) a promotion/relegation earned in the star's FINAL season loses its reveal banner — the heir inherits a new division with no explanation.** In `nextSeason` (`client/src/main.ts:1601-1615`) `setClubTier(newTier)` runs first (so the tier correctly persists), but when `age >= retireAge` the function calls `retireStar()` and `return`s BEFORE the `saveMgr({… lastTierMove …})` line, and the succession then `clearMgr()`s the manager state (`main.ts:1707`) — wiping any `lastTierMove`. So when the heir eventually begins managing (fresh season 1), the "⬆️ PROMOTED — welcome to X" / "⬇️ RELEGATED" banner never shows; the club has silently jumped a division since the player last saw it. (The transient toast still fires during `nextSeason`, but the persistent in-context reveal is gone.) Fix: carry `lastTierMove` onto the club/next-gen state so the reveal (or a "the club rose/fell to X during the handover" note) surfaces on the heir's first managed season.
- [x] **PT-28 [FIXED — the board's season verdict now judges against top-2/bottom-2, matching the real promotion/relegation rule (was top-3/bottom-3). main.ts nextSeason.] (stage: manager promotion/relegation): [low] (audit-based) the board's promotion/relegation zone (top-3 / bottom-3) contradicts the actual pyramid rule (top-2 / bottom-2).** `nextSeason` passes `promote: 3, relegate: 3` to `boardStanding` (`client/src/main.ts:1589`; the dead PvP path at `:939` uses `promote: 3, relegate: 2`), while the real rule promotes only the top 2 and relegates only the bottom 2 (`main.ts:1601-1602`) and the table's own colour key marks top-2/bottom-2. So the board can praise a 3rd-place finish as if it were promotion-worthy, or fret about "the bottom 3" when only bottom-2 go down — mildly misleading feedback sitting right next to a correctly-keyed table. Fix: pass `promote: 2, relegate: 2` (top-flight/basement aside) so the board's expectations match the rule the player is actually playing to.

<!-- Browser UX pass 2026-08-28 (manager transfers/contracts/incoming-bid) — REACHABILITY: no save is in the manager phase (no `fm_mgr_*` key in localStorage; all 6 saves — Marchetti incl. — are back at Academy in the player-career/heir phase, showing "Develop →", not "Continue the season"). Per the brief I did NOT grind a ~110-turn career; PT-29..33 are AUDIT-BASED, from reading the shipped transfer-market / contract-negotiation / incoming-bid render+logic (main.ts openTransferMarket/renderTransferMarket/buyPlayerFlow/sellPlayerFlow/openContractNegotiation/submitContractOffer/acceptStarBid; shared/src/transfermarket.ts, contracts.ts; client/src/api.ts) against the money-safety fuzz harness (shared/qa_transfer_fuzz.ts). No console errors on the menu/hub/facilities screens reached live. Deduped against PT-1..28 (which cover lineup/star/NFT/onboarding/league balance — none touch the transfer/contract/bid UI economy). -->

- [x] **PT-29 [FIXED — transfer market now uses MAX_SQUAD (28) / MIN_SQUAD (14) from shared, not a hardcoded 26; the counter shows /28 and the squad-range rule is stated. main.ts.] (stage: manager transfers): [medium] (audit-based) the transfer market's squad cap is hardcoded 26 in the UI but the real limit is MAX_SQUAD=28, so the last two legal signings are silently blocked and the counter mislabels the cap.** `renderTransferMarket` disables every Buy button at `squad.length >= 26` and prints `Squad {n}/26` (`client/src/main.ts:1146,1148`), while `api.buyPlayer`/`market.ts` allow up to `MAX_SQUAD = 28` (`shared/src/market.ts:11`, `client/src/api.ts:300`). A player at 26 sees all Buys greyed with "/26" and concludes the squad is full — losing two roster slots the engine would happily grant, and reading as a bug when the header says full but the rule isn't. (The sell floor `<= 14` at `:1147` does match `MIN_SQUAD=14`, but is likewise a magic number that should import the shared constant.) Fix: import MIN_SQUAD/MAX_SQUAD into main.ts and use them for both the disable tests and the `Squad n/28` label instead of literals.
- [x] **PT-30 [FIXED — Buy now routes through openConfirm ('Sign X for Nc?') like Sell, guarding the costlier irreversible spend. main.ts buyPlayerFlow/doBuyPlayer.] (stage: manager transfers): [medium] (audit-based) buying spends coins instantly with no confirmation, while the cheaper Sell asks you to confirm — the asymmetry puts the only unguarded, irreversible spend on the bigger-money action.** `sellPlayerFlow` routes through `openConfirm("Sell X for +Nc?")` (`client/src/main.ts:1164-1166`), but `buyPlayerFlow` fires `api.buyPlayer` and debits the fee on the first tap of "Buy · N,NNNc" with no confirm step (`:1151` handler → `:1154`). Buy fees are strictly larger than sell values (`transferFee > sellValue` by design), so the costlier, non-refundable action is the unguarded one and a mis-tap on a full-price signing instantly drains coins with no undo. The fuzz harness proves the math never goes negative but says nothing about a fat-finger spend. Fix: gate `buyPlayerFlow` behind the same `openConfirm` ("Sign X (OV n) for −N,NNNc?") used for selling.
- [x] **PT-31 [FIXED — disabled Buy/Sell buttons now carry a reason tooltip ('Squad full (max 28)' / 'Not enough coins (need Nc)' / 'Can't sell below 14 players'), and the squad-range is shown in the header. main.ts.] (stage: manager transfers): [low-med] (audit-based) disabled Buy/Sell buttons never say why, and the min-squad rule is never surfaced — at the squad floor every Sell greys out silently.** Buy is disabled on `coins < fee || squad.length >= cap` and Sell on `squad.length <= 14` (`client/src/main.ts:1146-1147`), but the disabled state carries no tooltip/reason, so "can't afford" and "squad full" are visually identical, and a squad at 14 shows a full column of dead Sell buttons with the 14-player floor mentioned nowhere on screen (the header only ever shows the buy cap). A naive player reads greyed buttons as broken UI. Fix: add a reason (title/inline note) on disabled buttons ("squad full", "not enough coins", "can't sell below 14") and state both bounds in the `tm-head`.
- [x] **PT-32 [FIXED — negotiateStar now charges the wage across the WHOLE deal (per-season wage × length), so a longer contract genuinely costs more up front; the modal shows the total and explains it. Length is a real trade-off now. api.ts + main.ts.] (stage: manager contracts): [medium] (audit-based) contract LENGTH is a dominant-strategy fake choice — the wage is charged ONCE, not per season, so the longest deal is always the best value and "a longer deal costs more" misleads.** `api.negotiateStar` debits `wage` a single time on accept and signs a `length`-season deal (`client/src/api.ts:350-353`); there is no recurring per-season wage. So for every personality the longest deal (6y) gives the most seasons of availability per coin spent — even a mercenary's `+0.14` length premium (`shared/src/contracts.ts:85`) tops out around base×1.56 for 6 seasons vs base for 2, i.e. cheaper per season — and a loyal player's negative premium makes a long deal both cheaper up-front AND longer. The modal sells a tradeoff ("a longer one costs more / less", `main.ts:891`) that has no downside, so the length selector (`main.ts:892`) is a decision with one correct answer. Fix: either make the wage a recurring per-season cost deducted at each rollover (so long deals genuinely lock in spend and short deals stay flexible), or reframe the UI so length reads as "seasons secured" rather than a cost tradeoff.
- [x] **PT-33 [FIXED — the haggle presets now span every outcome: Lowball 0.8× (reject/'he may walk'), Haggle 0.92× (counter/'he'll push back'), Meet it 1.0× (accept), Generous 1.18× (accept + bigger morale/loyalty). main.ts.] (stage: manager contracts): [medium] (audit-based) the negotiation's three offer presets can't reach the reject band, so "lowballing risks him walking" never happens and Generous is outcome-identical to Meet-it.** The modal hardwires exactly three offers — Lowball `0.9×`, Meet `1.0×`, Generous `1.15×` of his ask (`client/src/main.ts:894-896`) — against `evaluateContractOffer`'s thresholds `ratio>=1 accept · >=0.9 counter · else reject` (`shared/src/contracts.ts:98-103`). For any realistic star wage (ask ≫ 10) `round(ask*0.9)/ask` lands on ~0.9, so Lowball ALWAYS counters (never saves a coin, never risks the "insulted, walks away" reject the design promises), and Generous at 1.15 is a plain accept just like Meet-it — the only difference is a `+6` vs `+3` morale bump that the modal never mentions or explains. So the "real haggle" reduces to one button (Meet it); the risk and the generosity reward are both invisible. Fix: widen the presets (e.g. add a genuine `~0.75×` lowball in the reject band, or a free-entry wage field) and surface the morale consequence of a generous/insulting offer in the `cn-result` line.

<!-- Browser UX pass 2026-08-28 (top-end competitions — Continental Cup + World Finals) — REACHABILITY: no manager-phase save exists (localStorage has ZERO `fm_mgr_*` keys; all 6 saves in fm_saves are account stubs only, and the sole tier/heir keys are orphaned under a stale UUID handle at tier 5 with no matching mgr save). Per the brief I did NOT grind a full ~110-turn career; PT-34..38 are AUDIT-BASED, from reading the shipped continental/World-Finals render+logic (client/src/main.ts continentalHtml/playContinentalTie/simContinentalTie/resolveContinental/worldCupHtml/wcRunHtml/followWorldCup/wcTie/resolveWorldCup/showWorldCup + the nextSeason gating; shared/src/intl.ts contOpponent/worldCup/playerPath + the narrative banks). Structure is SOUND: knockout ties always resolve (seeded pens flag), the WC final always has a winner, playerPath is always defined for a qualified nation, contRound advances 0→1→2→3 with no off-by-one, editions start at season 4 = "Edition 1" (season init is 1, no Edition 0), and the QF/SF/F dot strips + promotion/relegation-style gating read cleanly. Two backend 500s appear on menu load (unrelated pre-existing api resource errors; the offline facade catches spSeasonReward/prestige). Deduped against PT-1..33 (none touch the continental or World-Finals stages). -->

- [x] **PT-34 [FIXED — the Continental Cup card's first round now explains what it is + how you qualified (top-3 in the top flight → knockout vs other nations' best; win QF→SF→Final). main.ts continentalHtml.] (stage: continental): [medium] (audit-based) the Continental Cup card never explains WHAT the competition is or HOW you qualified — the only "you're in" signal is a one-shot vanishing toast.** Qualification fires a transient toast on season rollover ("🌍 Top-3 in the top flight — qualified for the Continental Cup!", `client/src/main.ts:1635`), but the persistent card (`continentalHtml`, `main.ts:1349-1352`) only ever shows the tie ("🌍 CONTINENTAL CUP · Quarter-final vs <club> · rating ~14") with QF/SF/F dots and Play/Sim buttons. A returning or naive player has no in-context statement that this is a top-3-of-the-top-flight reward, that it runs alongside the league, that it's a 3-round knockout (QF→SF→Final), or what winning it grants (a coin prize + a continental title). The dynasty's marquee European strand reads as an unexplained side-panel. Fix: add a one-line explainer/subtitle to the continental card (esp. at round 0) — e.g. "You qualified by finishing top-3 in the top flight. Win three rounds to be crowned champions of the continent." (`main.ts:1349`).
- [x] **PT-35 [FIXED — nation flags now render throughout the World Finals report: group tables, knockout run chips, QF/SF ties, the final, and the champion line (were bare text). main.ts showWorldCup.] (stage: World Finals): [medium] (audit-based) flags appear on the World-Finals cards but are ABSENT throughout the tournament REPORT — the marquee screen shows every nation as bare text.** The teaser and live-run header use `flagImg(nation, …)` (`client/src/main.ts:1428,1438,1441`), but `showWorldCup` renders the group tables with a plain `${r.nation}` and no flag (`main.ts:1520`), the seeded-bracket `koTie` with plain `${t.a}`/`${t.b}` (`main.ts:1532`), and the played run chips + champion line with plain `${nation}`/`${r.oppName}` (`main.ts:1528,1530`). So the one screen a player studies at the emotional peak — four group tables plus the full knockout bracket — drops the flags shown everywhere else, making the recently-added flag presentation feel half-wired and the tables harder to scan. Fix: pass every nation name through `flagImg()` in `groupHtml`, `koTie`, and the run/champion chips inside `showWorldCup` (`main.ts:1518-1536`).
- [x] **PT-36 [FIXED — the report can no longer crown a nation the player ELIMINATED: realChampion now excludes any nation in your run's beaten-set, falling back to your eliminator then the final's other side. main.ts.] (stage: World Finals): [medium] (audit-based) the report can crown as "Champions" a nation the player already KNOCKED OUT in their own played run.** The star's SF/Final opponents are seeded independently of his actual results (documented design), and when a played run ends before the final the champion line falls back to the seeded `wc.champion` (`realChampion`, `client/src/main.ts:1524`). But the player's QF opponent (`path.qf.opp`) can BE the seeded overall champion — in the seeded bracket that nation beat the star and won it all, yet in the PLAYED run the star beat them in the QF. If the star then loses the SF, `showWorldCup` prints "🏆 Champions: <that nation>" directly beside a run chip showing the star beat them (`main.ts:1528-1530`). A keen player sees a team they eliminated lifting the trophy — a visible logic/immersion contradiction of the seeded-vs-played hybrid. Fix: in the `realChampion` fallback, exclude any nation already present as a beaten opponent in `wcRun` (pick the seeded runner-up / best remaining instead).
- [~] **PT-37 [DEFERRED — enhancement, not a bug: the contRivalClub/contRivalryBlurb 'old enemy' system exists but isn't wired to the continental opponent generation; repeat opponents feel random. Worth wiring later for continuity, but not blocking.] (stage: continental): [low-med] (audit-based) a full continental "old enemy" rivalry system is built in shared but never wired, so repeat opponents across seasons feel like random draws.** `intl.ts` ships `contRivalClub(seed)` (a stable per-save bogey club) and `contRivalryBlurb(...)` with dedicated win/loss banks (`shared/src/intl.ts:316-339`), explicitly to make recurring continental foes "read as a real rivalry rather than another random draw" — and CONT_POOL was widened to 30 for exactly this repeat-face concern. But both are called ZERO times in `client/src` (confirmed by grep; only `contTieBlurb`/`wcGroupDramaBlurb`/`wcKnockoutDramaBlurb` are wired). So drawing the same club two seasons running triggers no recognition, and the continental run has no season-to-season throughline the way the domestic rival (Turner) does. Fix: in `resolveContinental` (`client/src/main.ts:1374-1399`), when `tie.oppName === contRivalClub(leagueSeed())`, layer `contRivalryBlurb(...)` onto the tie card (the file comment already notes it's "ready for a future hookup").
- [x] **PT-38 [FIXED — the World Finals teaser now states it comes 'every four seasons' so its rarity is clear. main.ts (wcSeen was already recorded on conclude, so it's not silently unrecorded).] (stage: World Finals): [low] (audit-based) the "every-4-seasons" World Finals is silently skippable and unrecorded, its rarity is never stated, and its group table can't be reviewed mid-run.** Three thin spots dull a signature event: (a) the teaser (`worldCupHtml`, `client/src/main.ts:1427-1429`) says only "Edition N" — nothing conveys it stages every 4 seasons or that it's the bloodline's rare peak; (b) if the player never clicks "Follow the tournament", `nextSeason` wipes `wcStage/wcEdition/wcRun` on rollover without setting `wcSeen` or logging anything (`main.ts:1637`), so a whole World Finals can pass with no acknowledgement and no legacy trace (no `wcFinals`/`wcWins` bump, nothing in the trophy room); (c) during a played run `wcRunHtml` only prints "Group A winner → into the knockouts" as text — the actual group standings render solely at conclusion in `showWorldCup`, so the player can't review their group while the knockouts are live. Fix: state the 4-season cadence on the teaser, record an un-followed edition as a "missed"/simmed result in the dynasty log, and surface the star's group table in `wcRunHtml`.

<!-- Browser UX pass 2026-08-28 (front-of-house regression — MENU + SETTINGS + NEW GAME, no career needed) — played the whole front-of-house live at 1280/900/375px. EVERY settings control exercised and WORKING: Music slider (0↔100%, live %), Mute music (syncs the top-right speaker icon 🔊/🔇 both ways), SFX slider (0↔100%), Mute SFX, Reduce motion, CRT (scanline+vignette overlay actually renders), and UI scale (range 80–130% — at BOTH extremes the Close × stays reachable and the modal layout holds; at 375px the whole modal fits with all 7 controls visible, nothing clipped). Top-right speaker + gear buttons both work. NEW GAME: the live "<name>'s Club · the <name> bloodline" preview updates as you type, with a procedurally-selected crest badge (`/badges/badge-N.png`, 200 OK, inline-SVG onerror fallback); the input has maxLength=24 so a long name never overflows (scrollWidth==clientWidth confirmed). Readability floor OK — smallest text measured is 12px (mp-hint, save-date) ≥ the 11px floor; cyan wordmark + desaturated palette read as polished/premium, no clashing colour or low-contrast regression spotted. Only the 2 known pre-existing 500s on load (spSeasonReward/prestige, caught by the offline facade). The surface is genuinely solid — 3 minor NEW findings only. Deduped against PT-1..38 and the fixed M1–M9. -->

- [x] **PT-39 [FIXED — empty name now blocks Start with a hint (no silent "My Club").] (stage: menu/settings regression): [low-med] New Game with an empty name field silently founds a bloodline called "My Club" — no prompt, no guard.** The name field can be left blank: START stays enabled and clicking it (or pressing Enter) runs `startNewGame`, which does `const name = rawName.trim() || 'My Club'` — so a fat-finger or a player who expected to be prompted skips the signature "name your bloodline" beat entirely and gets a generic "My Club" that then carries down every generation, directly undercutting the core hook ("the family name carries down the generations"). why it hurts: the whole premise is the family name, and it's the one thing silently discardable at the very first screen · fix: disable START (or show a gentle "name your bloodline first" nudge) while `mm-name` is empty/whitespace, instead of defaulting silently (`client/src/main.ts:495`, listeners `:652-653`).
- [x] **PT-40 [FIXED — family name is capitalised (rossi → Rossi) in preview + saved club.] (stage: menu/settings regression): [low] the family name is never capitalised — a lowercase entry yields "rossi's Club · the rossi bloodline" in the live preview AND in the saved club name.** The preview echoes the raw trimmed value verbatim (`crest(raw + "'s Club")` / `<b>${raw}'s Club</b>` / `the ${raw} bloodline`) and `startNewGame` stores the raw trimmed string as the club name, so a naive player who types "rossi"/"a" gets a permanently lowercase, unpolished club + bloodline that reads as a bug on a premium title. why it hurts: club/family names are expected title-cased; the un-capitalised name persists into the hub, league table and every heir · fix: title-case the display and the stored name (`client/src/main.ts:646` preview, `:495` stored `name`).
- [x] **PT-41 [FIXED — name maxlength 24→18 so the header wordmark no longer wraps/cramps.] (stage: menu/settings regression): [low] at the 24-char max the New-Game club-name wordmark grows large, wraps to two lines and nearly collides with the crest and the flanking "Your club:" / "· the … bloodline" labels.** The maxLength cap correctly prevents true overflow (scrollWidth==clientWidth), but the preview wordmark uses a fixed large size that doesn't shrink for long names, so a max-length family name renders cramped/tight against its neighbours (visible with "Wolverhamptonshire-Feath's Club"). why it hurts: the preview is the New-Game screen's hero element and looks unfinished at long names · fix: scale the `<b>…'s Club</b>` font-size down as the name lengthens (or reduce the max a little) so the wordmark fits comfortably on one row (`client/src/main.ts:646`, preview typography in the stylesheet).

## Fixed (validated by the harness)

- [x] **PT-2 — top-flight titles too rare (~5%/season).** Top-tier transfer market now offers title-contender
  signings (headroom +3 at tiers 1–2) so an investing dynasty can build a squad that exceeds the league — an
  earned, coin-gated reward. Probe: title rate **5% → 18%**. `shared/src/transfermarket.ts`.
- [x] **PT-1 — ~24% of turns were a "bad hand"** (no fair-fit card, forced weak play). `ensurePlayableHand()`
  now swaps in a fair-fit card from the deck when the draw is dead; a dead hand only remains when the DECK
  itself can't answer (a deck-building signal). Probe: bad-hand **24% → 2%**, skilled Solid+ **71% → 94%**.
  `shared/src/career.ts`.
- [x] Core card loop: skilled play rewarded (Solid+ 71→94% vs random ~33%) — demand normalized by max not
  sum + variance cut. (was: perfect play routinely graded "Poor")
- [x] Rival beatable by good play (100% of skilled careers) after rate 6–9 → 3–5/turn. (was: impossible)
